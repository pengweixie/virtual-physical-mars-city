# HANDOFF · sci-cray-01 高能宇宙线测量站(2026-08-09)

设计册真源:`E:\Claude\mars-cray`(七本解析账 sim/01~07,**29 道闸全绿,其中 10 道是
已知答案闸**;README.md 有全账摘要)。

## 1. 交付物

- `viewer/units/sci-cray-01.js` — 10.3k 面,validate 0 FAIL / 2 WARN(均有意,见 §5)。
  24×24 m 取样地毯(16 模块)+ 抬升 1:1 剖切地层柱 + 大气厚度对照台 + 剖切数据方舱
  (屏蔽室五面板亦剖开)+ 事例显示屏 + 弃土堤 + 外围桩 ×8 + 场界桩 ×12。
  **零转动部件是设计特征**(与 sci-radio-01 / sci-seis-01 同族):原初粒子直接打到
  地面,没有需要跟踪的目标,spinners / oscillators 都留空。
- `viewer/units/sci-cray-01.info.json` — 8 张双语知识卡,8 个 poi_ 锚点零孤儿。
- `models/manifest.json` 一条(**pos:null**,rotation_deg 172、sink_m 0.6 已按候选 A 预填)。
- `dev/dev-preview-cray.html` + `dev/preview-cray.bat`(端口 **8128**,8123 保留)。

## 2. 落位候选(总控定点;两案都由引擎 sampleHeight 实扫选出)

在 `sci-radio-01 (-950,-545)` 周围 560–760 m 环带上扫 900 点(4° × 20 m),
评分 = 3×(32 m 窖芯高差) + (320 m 场区高差) —— 窖芯权重高,因为 26×26×5 m 的
窖底斜了,整层深度标度就歪。

- **A (-858, -1199) 推荐** · rotation_deg **172** · sink_m 0.6
  距射电阵 660 m(判据最小 566 m,余量 1.3 dB)· 距城区 1474 m · 距 sci-seis-01 697 m
  · 距 sci-swir-01 1007 m · **窖芯 32 m 高差 0.36 m = 全环带最平** · 场区 320 m 高差 5.57 m
  · 自射电阵看方位 8°(偏离对城常驻零陷 112°)· 布局审计 clean
- **B (-1513, -405) 备选** · rotation_deg **-76** · sink_m 0.6
  距射电阵 580 m(余量薄 1.1 dB)· 距城区 1566 m(比 A 更远离城市 UHF)· 窖芯 0.55 m

**两案共同要求**:`rotation_deg` 必须把 +Z(进场车辙与方舱开口面)**背向射电阵**,
让方舱金属壳体挡在数字机柜与射电阵之间。上表两个角度已经是算好的值。

城内烟测(临时 pos = A,测毕已回 null):scale = 1、36 资产共存、rotation 实测 172°、
泵帧 15 s、console 零报错;实拍 `mars-cray/shots/city_A_*.jpg`。

## 2b. 器件料单(**本站不新增器件研制**)

总控如果要问「这站要不要再开一版 SPAD」——**不要**。城里的器件够用,本站只出料单:

| 件 | 来源 | 本站怎么用 |
|---|---|---|
| SiPM 微胞 | `spad40_sentaurus` 40 µm NUV 胞 | **原样用**,一个新 mask 不开。PDE 目标 0.60 @420 nm 正对塑闪 425 nm 发射峰;需 6×6 mm 阵列 4128 只 ≈ 4.7 片 200 mm 晶圆 |
| 量程 | 同上 + 双增益 | **不开 25 µm 胞距的新变体**:40 µm 胞在 6×6 mm 上 22500 胞,Fe 主路吃 39% 饱和,但 1:30 衰减抽头只剩 1.8%;主路到 Z≈14 才越 10% 线,两路在 Z 4–10 重叠自互标定 |
| 读出前端 | `mars_spad_rox`(CRN65LP,IC618/Spectre 已签 ss/−100 °C 冷角) | **共用 PDK、HV 域划分纪律与冷角签核,不共用像素电路** —— 那颗是光子计数(淬灭+反相器出数字脉冲),本站要模拟电荷积分(4 个十倍程脉冲高度)。新增只有一颗 CSA+成形+双增益,仍走 ops-fab-01 同一条线 |
| 偏压环 | `spad40_mars` 的 BV(T) 表 | **本站唯一必须新做的器件级工作**。与 mars_spad_rox 的「固定偏压、不做 BV(T) 环」不是打架:那颗过压 40.7 V,2.1 V 昼夜漂移只占 5.2%;本站过压 6 V,同样 2.1 V 就是 35% → 增益同幅摆动,电荷测量报废 |
| `spad40_mars` 天文像素 / `mars_rad_sic` SiC PiN | — | **都不用**(前者是望远镜焦面的活;后者 MIP 在 50 µm SiC 里只有 ~5e3 e-h,留给中子哨兵) |

## 3. 报备总控的三件事

1. **供电**:本站 90 W,账 4 定版**城网主供、PV + 电池只做应急**。请在配电规划里
   给最终站址留一路低压支线(埋地)。
2. **施工期振动**:3380 m³ 挖方是真振源。本站建成后零转动部件,但**施工期需与
   sci-seis-01 排期**(该站红线是距 24 h 连续振源 ≥400 m,本站 697 m 达标,
   但施工是另一回事)。
3. **全城 HF 排放纪律加一条**:账 7 证明「纯距离永不闭合」——按 MIL-STD-461G
   RE102 合规的发射体要 566 m,比 RE102 高 40 dB(未屏蔽的商用 DAQ 就是这个量级)
   要 57 km。而射电阵的谱分辨 ~3 kHz,**一根落在 0.5–10 MHz 的时钟谐波比同功率的
   宽带噪声还差 5.2 dB**。建议把「0.5–10 MHz 禁设时钟基频与谐波」写进城市规范;
   本站已自我约束:站内参考取 **100 MHz** 而不是 10 MHz,与射电阵之间只走
   **1 PPS + 光纤**,全场 4140 路前端到方舱**没有一根射频铜缆**。

## 3b. 报总控的第四条(账 14 新增,**电路实测逼出来的**)

**前端 die 处 1–10 MHz 的电源纹波必须 ≤ 20 mV。**

出处:PSRR 在 IC618 上实测(Spectre / CRN65LP,tt + ss/−100 °C)——
SiPM 支路 @2 MHz 只有 **28 dB**(而且以 20 dB/decade 继续下滑),100 mV 纹波
等效 **0.12 个 MIP** 的电荷误差,直接吃掉前面所有 0.2% 级的漂移指标。

**这条和 §3 第 3 点打架**:为了躲开射电阵的 0.5–10 MHz,开关电源基频被推到 ≥2 MHz,
而前端 PSRR 正好从那里开始塌。**挪频率解决不了**(往高走 PSRR 更差)。
**定版解法(账 15 已在 Spectre 里逐方案实仿,不是手算)**:每片 ASIC 的电源入口做
**串联磁珠(~1 µH / 600 Ω @100 MHz)+ 10 µF ∥ 100 nF 阻尼并联去耦**(小电容串 0.5 Ω)。
实测全带 1–31.6 MHz 的 PSRR 61–95 dB,**最差等效电荷 0.0028 MIP**,压降 66 mV。

**上一版发出的「1 Ω + 10 µF」不够,请以本条为准**:10 µF 的 ESL(2 nH)让它在
1.1 MHz 以上变成电感,10 MHz 处比理想电容差 37.7 dB;而教科书式的「再并一只 100 nF」
在 10 MHz **反而更糟**(两支路反谐振,20.2 dB)。**加大容值和多并电容都解决不了,
必须靠串联磁珠把高频阻抗顶上去。**
请在配电设计里给每片 die 留磁珠 + 两只电容的位置与预算。

## 4. 卡片互引(需要动他人资产文件的,留总控/原 session)

本站卡已引 sci-pan-01(MiniPAN)、sci-rad-04(深地 μ)、sci-rad-01(剂量学 vs 天体物理)、
sci-radio-01(共址与授时)、sci-seis-01(授时链)、ops-fab-01(CRN65LP)、res-isru-01(塑闪基体)。
若要回边,建议两句:
- `sci-pan-01.info.json` 的 pan_net 卡:「同一条能谱往上,地表 sci-cray-01 在 10¹¹–4×10¹⁶ eV
  接手,两者在 1–2×10¹¹ eV 重叠 0.3 个十倍程,可交叉定标能标。」
- `sci-radio-01.info.json` 的 shelter 卡:「本站铷钟经 1 PPS 光纤同时为 sci-cray-01 授时;
  该站的数字排放按 §RFI 纪律受约束。」

## 5. 两个 WARN 都是有意的

- **MeshBasic ×1**:事例显示屏是 300+ 格逐格顶点着色,MeshBasic 是唯一低耗做法,
  且昼夜一致可读(同 sci-radio-01 瀑布屏、sci-rad-01 径迹屏先例)。
- **minY = −2.20**:外围桩与场界桩沉箱式下延。站址在远郊,320 m 场区高差 5.57 m,
  不下延会在坡下侧悬空(同 sci-radio-01 的 −2.80 先例)。manifest 给了 sink_m 0.6
  掩掉地毯基垫裙边。

## 6. 动画与验证

- `animate(t)` 纯 t 分段,**T = 150 s 超循环**,48 个确定性哈希调度的事例。
  命中的地表模块亮 → 剖切柱 6 层按级联下行次序依次亮 → 屏上出平面位置、能量/电荷
  标尺、到达方向天图、纵向剖面、Askaryan 波形(仅 >2.4×10¹⁴ eV)。
- 全循环包络扫描(0.05 s 步长):minY 恒 −2.20、maxY 11.63;**循环闭合差 0、
  同 t 复现差 0**;一循环 40 次模块点亮,点亮总时长 35.2 s。
- 屏上的**时间轴是压缩的,卡里写明了压缩比**:真实通量下 >10¹⁵ eV 一天一个、
  >10¹⁶ eV 73 天一个、>3×10¹⁶ eV 1.8 年一个;屏上每十倍程压缩约 20×,
  但**能量按真实积分谱的分位数取,分布形状是真的**。

## 7. 动图 ✓ 已拍

`snaps/anim/sci-cray-01.gif` — 10 s / 12 fps / 960×600 / **0.95 MB** / 120 帧 = **整 150 s 超循环压 15×**。

拍法(沿用 sci-rad-01/02、sci-uv-01 的先例,headless 工具在本机仍出白片):
城内可见页 → `renderer.setSize(960,600,false)` 固定画布(隐藏标签页下 canvas 会是 0×0)
→ 用 `group.localToWorld()` 按**资产局部坐标**定机位(局部 cam (-22,9,33) / target (-9,2.5,10),
与最终 rotation_deg 无关,换落位不用重调)→ 逐帧 `animate(t)` + 同步 `render()` + `toDataURL`
→ POST 到本地上传服务落盘 → ffmpeg palettegen/paletteuse 出 GIF。

**两条拍摄坑**:
1. **Sprite 要每帧隐藏,不能只隐藏一次** —— 引擎按距离重新显示 POI 标签,
   一次性 traverse 隐藏之后邻居的「低频射电天文阵」照样入画。改成在每帧 render 前重扫。
2. **白天拍不出动画** —— 事例辉光是 emissive,正午被日照冲掉。定版取 **ltst 18.4**(暮色):
   地形还看得见轮廓,而模块辉光、剖切柱 6 层、井盖紫光、方舱窗光全部读得出来。

循环闭合实测:相邻帧 RMS 120 对中有 68 对有明显变化,接缝(f119→f000)= 3.45,
落在这批变化里的**第 26 百分位**(中位 4.49)—— 接缝比典型帧间跳变还小,视觉上无缝。

顺带改了一处几何:剖切柱工作灯原在柱面前 2 m(range 22),夜里在柱面上烧出一个白斑
把 6 层琥珀板全盖住;改到 (-22, 6.5, 13) / range 15 之后层次才出来。

## 7b. 版图战役(账 16,交付后新增)✓

前端两级运放 `cray_ota33` 已完成 **版图 → DRC → LVS → xRC 提取 → 后仿** 全链:

| 项 | 结果 |
|---|---|
| 尺寸 | 76.9 × 23.0 µm(CRN65LP 3.3 V 器件,96 根栅指并成 8 只管) |
| DRC | **0 首过**(1535 条规则全零) |
| LVS | **CORRECT 首过**(7 端口 / 9 网) |
| 提取 | 最大寄生 9.3 fF(N2 网)= 补偿电容 2 pF 的 0.47% |
| 后仿 | 中频增益 +0.049%,上 −3 dB 11.22 MHz 不变 → **账 11 闭环结论原样成立** |

两道**工具闸**(相信"干净"之前先证明工具会说话):毒化版图(CO 0.09→0.07)
→ Calibre 报 2215 次 CO.W.1;毒化 CDL(M6 W 100→88 µm)→ LVS INCORRECT。

流程改判两条,已回灌进 ic-layout-signoff 的实践:
- **cen618 失联**(22 端口超时),而 minlei 一台机上 Virtuoso + Calibre + PDK 齐备,
  分裂流程塌成单机 —— "两台不得混用"防的是 PDK 混用,单机反而最安全。
- **OA 被整段绕过**:minlei 的 IC618 里 `techBindTechLibToDesignLib` 未定义,
  新建设计库绑不上 tsmcN65(只有 54 层默认 tech)。本 cell 全是矩形,照 layermap
  用 gdstk 直接写 GDS 即可 —— Calibre 只认 GDS,OA 只是中间商。

未画的(明说):RZ 1.2 k + CC 2 pF 补偿网络是独立的上层 cell(要另一套器件识别层,
面积比运放本身还大),`N2` 因此在本 cell 外露成端口。SiPM 支路整条前端的装配在后。

真源:`E:\Claude\mars-cray\layout\`(gen_ota33.py / cray_ota33.gds / cray_ota33.png /
poison_gate.py / pex/)。

## 8. 坑账(本 session 新增)

1. **`python -m http.server` 会中途死掉**:上一个 bash 会话结束时把它带走了,
   页面拿到 `chrome-error://chromewebdata/` 而不是报错。**导航后一定先 curl 验活**,
   再谈调试。8471 端口(上传服务)同理 —— 8462/8466 已被并行 session 占用,本册用 8471。
2. **剖切柱的板必须前后都探出**:板深取 `CD*0.9` 时它缩在土面之内,从观察侧完全
   看不见,整根柱子变成一块褐色板。改成 `CD + 0.16` 前后各探出 8 cm 才读得出剖面。
   ——「剖切」不等于「把东西放进壳子里」,得让它**穿出立面**。
3. **屏蔽室别做成实心盒**:第一版把 60 dB 屏蔽室画成一个 4.6×2.4×2.6 的实体箱,
   三个机柜全被它罩死 —— 剖开了外壳却在里面又做了一个黑盒。改成五面板 + 开口面
   剖开 + 外翻门扇。
4. **斜杆的 `rotation.z` 符号**:`box(w, L, d)` 的长轴是局部 +Y,`rotation.z = θ`
   把它转到 `(-sinθ, cosθ)`。要让上端落在 −x 侧需 `θ = +atan2(dx, dy)`,写成负号
   会得到一条**镜像的**切伦科夫射线(角度对、方向反),目检极易漏过。
5. **弃土堤要用「重叠的短段」而不是「等距的长段」**:第一版 9 段 × 17 m 在 46 m
   半径上按 0.575 rad 排,段间距 26 m > 段长 17 m,渲染出来是九块悬空的大板。
   改成 27 段 × 8 m、步长 0.115 rad(段间距 4.8 m)才连成脊。
6. **多本账之间的定版要回灌**:账 2 一度还停在「24 个独立井」的旧定版,而账 3/4
   已经按「一个整窖」算孔径与通道数。跨账不一致是最容易漏的一类错(sci-seis-01
   账 9 抓 ω0² 的教训同源)——收口前把每本账里出现的**共享尺寸**逐个对一遍。

7. **上机之前先跑纯几何自检**:同层矩形"既不相交也不共边、间隙却 < 0.15 µm"几乎
   必是坐标算错。账 16 的生成器靠这一段在上机前抓出三处落点错误(栅焊盘骑上相邻
   梳齿 0.015 µm、焊盘落在对侧栅组、横轨没铺满留下一排孤儿过孔)。
   一轮 VM 往返几分钟,一次自检几十毫秒 —— 这是账 15"磁珠接线错"教训的直接兑现。
