# REPLY — sci-rad-01:账 19 预注册 —— AFE 高电荷端 ToT 饱和,在 Spectre 跑之前写死闸与预期

**册子:** `E:\Claude\mars_rad`。预注册记于 **`d9c6f83`**(回执 §206;存根 `sim/19_afe_tot_saturation.py` 的 `GATES` 表即注册,实现不得改阈值)。上一状态 `049d933`(LET 谱轮收口)。
**日期:** 2026-09-09。答用户指令「AFE 高电荷端 ToT 饱和先开一本账,预注册后再算」。

## 为什么是这本账

账 14 的扰动扫描里,每像素电荷硬截断 1e5 / 3e5 / 1e6 e 让回收的剂量当量只剩真值的 0.21 / 0.24 / 0.32——这是 LET 谱轮最大的系统差,而冻结的器件检查点只在 q200–q500 表征过前端。1 GeV/u 的 Fe 正入射在一两个像素里放 ~23 Me。这本账用器件战役自己的冻结晶体管网表量真实的 ToT(Q),200 e 到 30 Me。

## 先读到的(器件战役 E:\Codex\2026-06-21\timepix4_allpix)

冻结前端 = `frozen_hybrid8_gated_*` 网表族(2026-07-15)+ 晶体管级验证报告:TSMC65 RF-MOS、1.2 V;堆叠反相器 CSA,Cfb 1 fF,100 MΩ 直流反馈;跟踪 PMOS 镜像反馈,放电目标 50 nA,由 hit 门控;hit 驱动的基线复位;6 位 R-2R 阈值 DAC + 无源混合器;五管差分甄别器;探测器电容 50 fF;全像素增益 49.56 µV/e;冻结 ToT 7.6–25.8 ns @ q200–q500(30 事件端到端)。注入是电流脉冲(升 5 ns / 宽 1 ps / 降 10 ns),电荷 = val1 × 7.5 ns(在 q300e 网表上核过:6.4087 nA × 7.5 ns = 300 e)。瞬态只跑到 400 ns、最大步长 50 ps;30 Me 事件在 50 nA 放电下要 ~100 µs,扫描网表两者都得重标。

**跑之前登记的事实:冻结网表没有输入保护二极管。** 凸点节点上只有 100 MΩ 电阻和镜像 PMOS 的漏极,没有任何结到地。没有保护的网表会让输入节点随电荷任意摆、报出一个真实像素不可能有的电荷量程。所以路线 B 跑两个变体——照原样,和加一个声明的保护结——原样曲线在输入摆幅越过二极管正向压降处即标「不物理」。

## 路线与规则

- **B(跑得起来就是答案)**:Spectre 18.1,Cadence 虚拟机 MINLEI8(目前关机;vmx = /f/IC618-OK 那份,MAC 与 skill 表一致),14 个电荷点对数扫 200 e–30 Me,提取 hit 高电平时长(ToT)、CSA 输出峰值、输入节点摆幅、保护结抽走的电荷(电流积分)、重触发次数。
- **A(行为模型,必跑,L1)**:ToT = t_disc(Q) + max(0, Q − Q_lin)/I_fb,再加 Q_clamp = C_node × V_diode / e 的钳位;参数取自网表不取自 B;拿来对照 B。
- **规则:B 跑得了,B 是答案;只有 A 则标 L1,本项不闭合。**

## 七道闸(`GATES`,阈值写死)

| 闸 | 查什么 | 阈 | 登记预期 |
|---|---|---|---|
| G19.1 | 已知答案:B 在 200/300/500 e 复现冻结 E2E 的 ToT | ±10% | 绿(同网表族;只证明跑的是同一个东西,无权重) |
| G19.2 | 线性区 Q_lin(斜率恒定 ±10%) | 报 Q_lin | 5–15 ke(CSA 输出摆幅 / 49.56 µV/e) |
| G19.3 | 恒流区:Q_lin 以上斜率 e/I_fb = 3.2 ns/ke | [2.6, 3.8] ns/ke | 绿,直到钳位 |
| **G19.4** | **钳位**:原样网表输入摆幅越过 −0.6 V 的 Q_x;加结后的电荷损失份额 | 1 Me 处损失 ≥70%,ToT 平台 | **Q_x 150–400 ke**(50 fF + 栅电容,0.6 V),平台 ToT 0.5–1.5 µs —— **本账的核心数** |
| G19.5 | 30 Me 内单脉冲、无重触发 | 重触发 0 | 不确定;高电荷重触发是发现不是 bug,上卡 |
| G19.6 | 14 位 @40 MHz 计数器溢出 | 报 Q_overflow | 128 Me @50 nA,无关:钳位先到两个量级 |
| **G19.7** | **回灌账 14**:用实测 ToT(Q)+钳位替换硬截断,1.44 族 H 回收 | 三种:照实测 / I_fb ×10 / 外围像素重建(σ_plasma 0/20/50 µm) | 照实测 0.2–0.35;**I_fb ×10 无用**(限制是钳位不是斜率);外围重建只在 σ_plasma ≥ 50 µm 时救回 H ≥ 0.8 —— 决定性扫描 |

声明的、不出自战役的量:保护结(PDK n+/p-sub,10 µm²)、重离子柱的横向电荷展宽 σ_plasma(等离子体效应不在检查点里)、ToT 计数器(14 位 @40 MHz,数字选择)。**赌注最大的预测是 G19.7 中间那行:提高反馈电流救不了**——限制在放大器打到轨之后电荷去了哪,不在放电多快。错了,便宜的修法就在;对了,修法在传感器侧或重建侧。

## 次序

本件 → 起虚拟机、造扫描网表、跑 Spectre → 提取、七闸、回灌账 14 → 回执交城仓(引本仓提交号)→ 卡(LET 谱产品面板卡的「最大系统差」一行按实测改)。城仓不 commit。

六闸绿于存根。分母 2.5。
