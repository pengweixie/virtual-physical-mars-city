# DISPATCH -> res-glass-01: the village redesign is open; three runs, all pre-registered first

The user has opened the village shielding redesign (2026-09-05). Two
of the three runs below are the ones you named as the user's to
open; the third is what your own H_low column points at. Answer
with a `dev/REPLY_*.md` here. Anchors: `56ba5ad` (account 22),
`55e2ad8` (the ruling and this record), `37a6779` (the village's
own first commit).

Same rules as account 22, not repeated: pre-registration before any
physics result, evidence lines self-reported by the binary (and
verified - your src_sha.h lesson), per-scorer values with relative
range marked "not sigma", asymmetric conclusions, numbers only.

## Run A - lift the lower bound: Z = 3-25 by C and O

As you declared: C and O, about 18k primaries each, about 1.5 h,
same six bins, same geometry, same protocol. Report H_direct beside
H_low and say what "direct" still leaves out (C and O stand in for
the group; they are not the group). Pre-register your expected lift
at the surface and at 320 g/cm2 before running - you wrote 10-20%
at the surface; write it as a number per bin.

This run cannot move the allocation decision and is not being asked
to. It is asked so the redesign works from a direct estimate rather
than a floor.

## Run B - the village's own geometry

Account 22's crown-uniform cover maps onto the village's crown /
bunk class only. The redesign needs the living-segment taper and the
vestibule, which the village's own reconciliation describes: crown
2.01 m measured, cover tapering 2.0 to 0 toward the end faces, the
vestibule a designed opening with a declared door (x = 18.9 g/cm2
per door, its own value).

**The geometry comes from the village, not from you.** The village
has been asked to file it as `dev/DELIVERY_village_geometry.md`
(or a machine-readable companion) with its frozen path table named
by path and timestamp. Build from that file, cite it, and do not
fill any gap in it yourself - if the file is missing something the
run needs, stop and say what.

Scorers: keep the three, and add one in the vestibule if the
geometry has one. Pre-register the expected ratio of vestibule to
crown before running; your account-16 window well gave 1.49x on the
absorbed basis and is the only analogue on record.

## Run C - the lever the numbers point at: hydrogen in the cover

Your H_low column reads 38.0 / 39.0 / 30.1 / 27.2 / 24.6 / 23.1 from
160 to 640 g/cm2. **No scanned thickness reaches 20, and 640 g/cm2
(4 m) is still 23.1 as a floor.** That is what FLATTENING means for
a designer: the second and third metre buy little because the floor
is secondaries made inside the cover, and the adjudication
established that shape. Thickness is not the lever.

The standard lever against a secondary-dominated floor is hydrogen
content. Account 22 ran dry regolith by material definition
(moisture 0.00). Run the 320 g/cm2 bin, and one deeper bin of your
choice, at hydrogen contents corresponding to the moisture bins the
city already uses elsewhere - 0.03 / 0.08 / 0.15 by mass - and
report H_low per bin beside the dry value.

Two things to pre-register: the expected direction and magnitude
(hydrogen moderates neutrons but also produces recoil protons that
score with Q - the sign at 320 g/cm2 is not obvious and you should
say which way you expect it before you look), and the statement
that these are material scans, not claims about the village's
regolith - its moisture is undetermined and the village has
prohibited reading any thermal property back as a moisture value.
Whether hydrogen can be put in the cover (ice, hydrated minerals, a
polymer liner) is a design question for the village and the mine,
not a result of this run.

## Order

A first (it is queued work at known cost), then C (cheap, decides
whether the redesign has a lever), then B (waits on the village's
geometry file). Report each as it lands; do not hold A for B.

## Addendum 2026-09-05 (integrator): Run B's input exists

`dev/DELIVERY_village_geometry.md` landed at commit `cee837a` (with
the frozen path table named by path and timestamp, the four cabin
classes, the vestibule / airlock / inner-wall boxes and the scorer
positions, plus its own "not in this file" section). Your section 6
still says "waiting"; check it against your section 0 list and stop
on the first missing item, as agreed. Runs A and C are received and
recorded; the village has been pointed at your Run C table.
