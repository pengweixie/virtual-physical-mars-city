# DISPATCH -> res-glass-01: Run E - the vestibule works and the moved living position, in the village's geom-r3

Queued behind the village's next geometry file; pre-register first,
as always. Anchors: `b2077f4` (Run B), `8c51f79` (Run D), the ruling
`dev/DISPATCH_village_ruling_three_levers.md`.

## What the user decided

All three remaining levers: vestibule works, the living function
moved to the crown depth, the stay limit as policy. Hydrogen is
closed as design and is not part of this run. Run E is the
verification of the first two in the village's own geometry.

## Input

`dev/DELIVERY_village_geometry_r3.md` from the village - geom-r2
plus its vestibule proposal (cover over the airlock box, thicker
screen wall, longer labyrinth, or the subset it chooses) and the
candidate living position z = -1.0. Build from the file; stop on the
first missing item and name it, as in Run B. Do not fill a gap.

## Receptors

Run B's three (bunk, living, inside the airlock) plus the new living
position, plus one in the vestibule after the works, so the before
and after of each lever is a paired difference on shared seeds -
your own primary statistic since Run C, and the heavy-tail
statistic you registered for Run D. Two seeds from the start.

## Pre-register

- Expected ratio of the vestibule receptor after the works to
  before (93.7), per option the village delivers; and the expected
  ratio of the new living position to the old (52.8), against Run
  B's bunk/living ratio as the only analogue.
- The lever criterion in the asymmetric shape used since Run C:
  what counts as "the works are a lever" and "the move is a lever",
  in paired sigma, both classes, same sign.
- What Run E cannot say, written before data: it does not test the
  bunks (they do not move), and it does not touch the stay-limit
  lever, which needs no run.

## Report

Numbers only; the village re-issues its stay-limit table on your
result and the integrator rules. Provenance line self-reported by
the binary and verified, per your own src_sha.h lesson.
