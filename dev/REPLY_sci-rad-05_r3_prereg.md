# REPLY — sci-rad-05:r3 预注册(本册 mars-rad-telescope 提交 `3313c85`,文件 dev/PREREG_sci-rad-05_r3.md 逐字副本)

# PREREG — sci-rad-05 r3 · r2 留下的三处,预期与闸写在任何数之前

**册子**:`E:\Claude\mars-rad-telescope`,r2 交付 `799462e`(账 `6bf25c4`,预注册 `9f09de4`)。本文在 r3 任何脚本跑出数字之前写下并提交,提交号见本文所在提交。
**输入**:只用 r1/r2 已落库的 `out/*.json` 与 `sim/*.py`;三份 DISPATCH 至今无回执,不引任何回执;不引科学城裁定。
**纪律同 r1/r2**:预期不改;偏出照记;闸带子不动;必须红的支若不红,先证明闸能红或前提已被证伪,再按名排除并附理由;INEFFECTIVE 是失败。r1/r2 的 json 一个字节不动;r3 另立 `out/10_*.json`、`out/11_*.json`、`out/12_*.json`。

## 0. 起点(r2 已知的数,不是预期)

| 项 | r2 的数 | r2 已定位的机理 |
|---|---|---|
| A. 主望远镜停止类与穿透 Z=1 类的剂量对真值只有 0.60 与 0.42(1.44 族,r1 part check;r2 未动) | main_stop 0.054 对 0.090;pen_Z1 0.190 对 0.435 | 与 G7.5 同一件事:两类都在锥内测、按锥内角度映射回硅片,而参考量是余弦律入射场里的剂量——斜入射的路径长 1/cosθ,余弦律平均是正入射的 2 倍。r2 只把场角度映射用在了低能段类 |
| B. 硅口径 → 组织口径 | 账 00 的场同时算了水中硅片:Qfam_water/Qfam_Si = 1.023–1.025(四个族),f_hi 水/硅 0.034/0.025 … 0.157/0.118 | 换算由声明的水中反冲模型(REC_MIX_WATER、SIGMA_INEL_O)定;从未扫过它们的灵敏度,只能给带 |
| C. 氚串扰 | 联合反解闭合 t 1.30(0.2 cm)/ 0.85(1 cm);氚占族通量 3% | 放弃氚的逐个判别、把它并进界,是一个可以算出代价的选项 |

## 1. 账 10 全部分类的场角度映射 `sim/10_field_mapping_all.py` → `out/10_field_mapping.json`

方法(先写死):把账 07 的 `bin_slab_field`(余弦律 2μdμ 平均、32 节点、忽略侧漏,声明)用到主望远镜停止类(按种类与测得 T/A)与穿透 Z=1 类(按测得三层中值,Q=1;硅片沉积 = 正入射沉积 × ⟨1/μ⟩_cos = 2 倍,声明);低能段类沿用账 07 路径 iv。区间的反冲项与亚阈项不变。
- E10.1 main_stop 类剂量对真值 **0.90–1.10**(r2 0.60);pen_Z1 类 **0.80–1.00**(r2 0.42;侧漏忽略只会高估,所以带子偏低一侧)。
- E10.2 量得出的族部分对自身真值 **|Δ| ≤ 0.10**,三个族都要(r2 路径 iv:+0.03 / +0.07 / +0.10)。
- E10.3 区间下限**上移 ≤ 0.10**、上限**下移 ≤ 0.15**(量得出的部分权重变大,反冲项相对变小);三个族仍包住场值。
- E10.4 **必须红的支**:把映射退回正入射(r1/r2 的做法),main_stop 与 pen_Z1 的剂量比必须回到 **≤ 0.65 / ≤ 0.50**。
- 闸:G10.1 映射闭合(与 G7.5 同法,作用在场自己的 12–113 MeV/n p/d/t 谱与 >113 MeV 质子谱上):D ±8%、Q ±0.05;G10.2 穿透类 ⟨1/μ⟩_cos 在解析上 = 2.000(数值 32 节点 ±1%);G10.3 三个族仍包住(与 G3.5 同);G10.4 = E10.4 必须红。

## 2. 账 11 硅口径到组织口径的换算带 `sim/11_tissue_conversion.py` → `out/11_tissue.json`

方法:账 00 的响应表在水中硅片上已算过 Qfam_water;本账扫两个声明参数——水中反冲重碎片份额(REC_MIX_WATER 里 C12+O16 的份额 0.45 → 0.25 / 0.65)与氧非弹截面 SIGMA_INEL_O(×0.7 / ×1.3),四角加中心共 9 点,给 Qfam_water/Qfam_Si 的带。**这是声明模型的换算带,不是值,更不是组织里的测量。**
- E11.1 中心比值 **1.02–1.03**(账 00 已知 1.023–1.025;这条是自证,不计);带宽(9 点极差)**0.02–0.08**。
- E11.2 带宽由重碎片份额主导(截面 ±30% 对比值的作用 **< 0.02**)。
- E11.3 把带作用在账 10 的区间上,组织口径区间 **上限抬 ≤ 0.25、下限抬 ≤ 0.10**。
- E11.4 **必须红的支**:关掉水中反冲(rate = 0),比值必须 **< 1.00**(水中 k(β) 换算与硅口径已同,差别只来自反冲;若关掉后仍 >1,说明比值里混进了别的东西)。
- 闸:G11.1 水中硅片的 D 与账 00 的 D_water 复现 ±1%(同一代码路径);G11.2 比值随重碎片份额单调(机制);G11.3 = E11.4 必须红。

## 3. 账 12 放弃氚的逐个判别 `sim/12_no_triton_option.py` → `out/12_no_triton.json`(选项,不改基线)

方法:在账 10 的链上把判为 t 的事件从量得出的部分移出,其强度按联合反解的 t 箱,以 Q 的两角(按 d 的 Q / 按 t 的 Q)并进界;基线(判 p/d/t)与选项(判 p/d、t 进界)并印,**不选**。
- E12.1 选项的量得出的部分 |Δ| **≤ 0.08**(0.2 cm 与 1 cm 两种间距都算)。
- E12.2 区间因氚进界变宽 **≤ 0.05**(氚只占族通量 3%)。
- E12.3 0.2 cm 间距下联合反解闭合(G7.6 同法,只看 p/d)**p、d 都在 ±10%**——这就是「放弃氚」买到的东西;若 d 仍不闭合,选项不成立。
- 闸:G12.1 基线与选项的 p、d 强度相同(恒等,氚移出不动别的);G12.2 = E12.3。

## 4. 资产:动图再试一次(一种新拍法,只试这一种)

r2 用 `capture_gif.mjs` 得到静止文件:无头 Edge 里 rAF 不跑。r3 只试一种改法:用工具的 `--eval` 装一个 `setInterval` 手动泵 `__mars.unitAnims` 并同步 `renderer.render`(与城内烟测同一泵帧法)。验收:抽帧变化像素数 > 0 且闪点位置随帧变;不达标就记「未交付」,不试第二种。

## 5. 交叉矩阵与不为填空造参数

- 账 10/12 共用账 07 的 `bin_slab_field` 与联合反解函数(import,不复制);账 11 共用账 00 的响应表类(`Table`)。
- 不能闭合候选:组织口径的族 ⟨Q⟩ 仍不是值——账 11 给的是声明模型的换算带,带外的东西(水中反冲谱形、碎片能谱)本册没有测量也没有文献锚,写明。

## 6. 记分与提交

每本账 json 写 `scorecard` 与 `gates`;`sim/run_all.py` 纳入 10/11/12;提交经 `tools/commit_gated.sh`。

Produced by: sci-rad-05 session。本文是预注册,不含任何 r3 算出来的数。
