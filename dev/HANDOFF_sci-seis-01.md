# HANDOFF · sci-seis-01 火震监测站 + sci-seis-net-01 远端节点(2026-08-07)

设计册真源:`E:\Claude\mars-seis`(五本账 sim/01~05,22 闸全绿;DESIGN.md 有全账摘要)。

## 交付物
- `viewer/units/sci-seis-01.js` — 主站 9.3k 面,validate 0 WARN。WTS 风热罩(裙边压石)/
  吊罩展示位+真空球 100° 剖切露三支 VBB 倒摆/夯实安装座+调平墩/负载分流系缆/电子柜/
  太阳板/实时震波屏(3 通道×56 列,确定性哈希)/信标(blinkMats)/发射方位指向牌。
  **零转动部件是设计特征**(安静即仪器)。
- `viewer/units/sci-seis-net-01.js` — scatter ×2 变体(打入桩 348 面/基岩锚 384 面),
  手写等价自检过(validate 不支持 scatter 形态,同 env-scatter/weather-net 先例)。
- 知识卡:`sci-seis-01.info.json` 7 张 + `sci-seis-net-01.info.json` 2 张,全双语,
  sim/physics 均锚设计册账。
- manifest 两条已登记(pos:null 待总控);`snaps/anim/sci-seis-01.gif` 动图;
  预览页 `dev/dev-preview-seis.html`(?unit= 通用模板,8126 端口验证)。

## 发射联动(无引擎缺口)
`meta.schedule = { action:'接收发射信号', ltst:14.0 }` 与 veh-rocket-02 发射同刻,引擎
现有 scheduled 通道自动触发;animate 内 +2 s(P 波走时)后屏上出现 45 s 发射特征波形、
状态灯转红。环视面板亦有「▶ 接收发射信号」手动按钮。**未改 main.js。**

## 落位候选(总控跑 audit 定点;红线:距城区主要振源 ≥400 m,两案均满足)
- **A (-700,-520) 推荐**:西南科学区。距最近振源(fab 入口)614 m、矿场 967 m、
  聚变泵 792 m;SP 夜残余 10.8×(全城可选点里最安静);距发射工位 1642 m,
  单发 SNR ~2900 仍充裕。烟测实照 mars-seis/shots/c2_city_framed.jpg——此点有缓坡,
  建议 sink_m 0.30~0.35 并允许 ±30 m 挪到局部平点。
- **B (250,-560) 主动源优先**:距振源 ≥441 m;距发射工位 952 m,发射信号强 10×
  (浅结构反演更利),代价是 SP 夜残余 23.4×(比 A 吵一倍)。
- **矛盾的平衡写明**:避开发射直线路径(羽流喷砂/冲击波走廊)但留在 1~2 km 内收信号;
  两案都在工位西南象限、不在下射程线上。若科学优先取 A,标定优先取 B。
- 城内烟测(临时 pos=A)已做:scale=1、31 资产共存、泵帧 20 s 168 柱全动、
  发射动作满栅、console 零报错;测毕 pos 已回 null。

## 散件撒放建议(引擎 scatter 撒放仍硬编码 env-scatter-01,同 weather-net 待总控)
主站定点后 1.2~2.2 km、方位互隔 ≥90° 撒 3 桩(A 型×2 + B 型×1,B 型放基岩露头/崖边);
依据:24h 源 8 Hz 残余压进夜地板需 877 m(Q15)~1414 m(Q25)+ 三站二维慢度最小闭合。

## 报备总控的两件事
1. **卡片互引回边**:本站 launch 卡已引 ops-spaceport-02;若要工位卡回引本站
   (「起飞冲击=sci-seis-01 的标定源」一句即可),需动 ops-spaceport-02.info.json
   ——他人资产文件,留总控/原 session 加。
2. **给城市的隔振规格**(账 3 反向输出):24h 连续机械(ISRU 压缩机/聚变泵/地下厂)
   隔振到 ≤1.2e-6 m/s/√Hz@10 m,夜间 SP 残余才有上表的量级;建议进城市工程规范。

## 坑账(本 session 新增)
- 并行 session 端口双绑:8124/8462 被 swir/uv 占用,`python http.server` 在 Windows
  上双绑不报错、先起者赢——**起服务后必须 curl 验证内容归属**,本册改用 8126/8466。
- 震波三通道事件调度种子曾各自独立 → 地震只出现在单通道(物理错误);
  修法:调度种子与通道无关(共时),ch 只进相位与幅比。金标准 sim/05 同步修。
- 屏滚动窗 15.7 s:动图段长必须 ≥ 事件时长+滚动窗(录 42.8 s setpts 压 12 s),
  否则尾波没滚出屏、循环接不拢。

## v2 深挖轮补充(08-07 同日)
- 新账 sim/06 反馈链(5 闸全绿):无补偿温漂 ±3.7e-3 m/s² = 开环满量程 5×
  (摆每天下午打止挡)→ TCDM ÷20 + 力反馈钳位 0.13 µm + VEL/POS 双增益档必然性。
- 几何四件:VBB 单摆 6× 剖视教学台(金/铜/钢/白同色因果链,台座色标条)、
  SP 三分量剖切盒(三轴色=震波屏三通道色)、E-box 检修门外翻(ADC/反馈/采集板+加热片)、
  吊罩内面 DoubleSide+蜂窝肋(修从下看内面剔除穿帮)。10.9k 面 validate 0 WARN。
- 卡 7→8(新 feedback 卡锚教学台,vbb 卡 sim 转数组);poi 锚 9(poi_sp 惰性锚,坑账 23)。
- 城内回归(临时 pos=A,已回 null):scale=1、泵帧 10 s、console 零报错。

## v3 深挖轮补充(08-07 同日,累计 8 本账 41 闸全绿)
- 新账 sim/07 定位精度(6 闸):单站偏振 σ_baz≈1/(SNR·sin i),本地 2.8°/区域 8.4°/
  远震 25.5°;S-P 测距 200 km 外速度模型主导拾取项;**1.5 km 阵列对远震 21.6° 比单站
  还差、对面波 2.7°** → 远震归单站偏振、本地归小阵列,这条分工定死远端节点 1.2~2.2 km
  的撒放尺度(与散件卡口径一致);撞击坑轨道影像反向标定 5%→1.4%。
- 新账 sim/08 功率-生存(8 闸):PV 全负载 sol 474 掉线、τ=5 尘暴当天失守 →
  **太阳板是应急备份,主供电接城网**。矛盾:电缆是供电通道也是振动通道;
  架空+分流环 与 贴地无分流环 殊途同归 2.0e-9(本底 53×),两者齐上才 2.0e-11。
  数据 80 MB/sol,城内链路允许 VBB 全速率连续记录(InSight 只能 2 sps)。
- 几何两件:**方位定位盘**(指针=最近事件反方位角,与屏共享调度种子;置信弧宽度=σ_baz,
  三档 visible 互斥;红标=发射工位 56°,三青标=远端节点)+ **城网接入柱**(埋地沟→
  混凝土隔振墩+弹性垫→立式 Ω 环→双路配电箱 绿/琥珀→E-box)。12.0k 面 validate 0 WARN。
- 卡 8→10(location / power),poi 锚 11。城内回归 scale=1、31 资产、console 零报错。
- **给总控的新输入**:账 8 要求本站接城市电网。请在配电规划里给 (-700,-520) 或最终站址
  留一路低压支线;线路请走埋地并避开主要振源直线——这条支线本身也是振动通道。
- 坑账新增:浏览器面板收起时 window.innerWidth=0 → canvas 0×0、toDataURL 返回 'data:,'
  (6 字节)。修法:截图前 `renderer.setSize(1280,800,false)` 再同步 render,别依赖
  resize 事件(坑账 11 的城内变体)。

## v4 信号链轮补充(08-07 同日,累计 10 本账 57 闸全绿)
- 新账 sim/09 响应与标定(8 闸):开环 Q 峰 ×199.9 不可用 → 反馈压平 88.7 dB =**反馈的
  第三个理由**;**相位精度就是走时精度**,账 4 的 0.28 ms 反推标定规格 0.1°;环增益
  0.1 Hz 1068 → 5 Hz 6.3 = VBB 主带止于 ~2 Hz、高频交给 SP 的定量身份。
- 新账 sim/10 数字链与授时(8 闸):Σ-Δ 让模拟抗混叠只需 2 极点;抽取 FIR 群延迟 1.92 s
  可扣、**前驱振铃扣不掉**(毁初动极性);**火星没有 GNSS** → 站上 OCXO + 每火星日经
  UHF 与 sci-radio-01 铷钟同步 + 双程测距扣 1.4 µs 光行时。
- 几何三件:信号链展板(八级色块=实物件色,铜线折回明示反馈是环)+ 标定线圈 +
  OCXO 授时单元(含朝 com-station-01 的授时天线)。12.9k 面 validate 0 WARN,卡 10→12。
- **自查抓到真 bug 并已修**:账 6 的 ω0² 写错(1.4685 应为 6.183),被账 9 的跨账一致性闸
  炸出。超量程倍数 5.1× → **1.21×**、残余位移 0.127 → **0.030 µm**;结论不变,卡与台账已回改。
- **给总控的新输入(第二条)**:授时。本站需要每火星日一次与城市时间基准的同步,
  建议挂 sci-radio-01 的铷钟经 com-station-01 的 UHF 分发;若城市尚无统一授时服务,
  本站可先自守 OCXO(裕度 3.2×),但跨站联合定位与主动源叠加都依赖这条同步链。

## 账 11 收口轮补充(08-07 同日,累计 11 本账 65 闸全绿;纯账,无新几何)
- 新账 sim/11 端到端「你实际看到什么」(8 闸):把前十账串成真链路并出三张图集
  (fig/11_traces / 11_spectrogram / 11_precursor)。
- **账 2 的图像兑现**:同一本地 M1.5,夜 SNR 5.5 看得见、昼 0.03 完全淹没(171×)。
- **谱图签名是分类的真办法**:本地=脉冲宽带 / 区域=低频长尾 / 发射=持续窄带双线
  (1.8+2.7 Hz,机器不是地震)/ 标定=一条干净斜线。这套签名可直接写进未来的事件
  自动分类器规格。
- 反卷积:水位法(|H|<2% 不除),高 SNR 相关 0.9999/峰值误差 0.22%,低 SNR M1.5 仅 0.69
  —— **反卷积不创造信噪比**。
- **第二处跨账一致性闭合**:账 10 抽象估前驱可见度 ≈3,账 11 端到端算 M4.7 级前驱 =
  夜地板 2.90 倍。由此把账 10 的笼统说法收紧成可操作门槛:**日常 M4 事件前驱仅本底
  0.58 倍不必管,M4.2 以上才需要最小相位流重处理**。
- 本账翻车两次已入册:①P 子波用 Ricker(对称非因果)→ 测到的"前驱"是子波自己的前半支;
  ②`np.convolve(mode='same')` 对对称核已零相位,再补群延迟会把前驱窗滑到 S 波上。
- 口径声明:账 11 的 SNR 是**全带 rms**,账 4 的 2894 是**窄带匹配滤波**口径,
  两者差的是处理增益 —— 引用时不要混用。

## Fable 评审轮(08-07,自审,8 发现全处置;闸 65→69 全绿)
- **高 ×3(全部是跨账不一致)**:①账 11 发射幅度比账 4 低 231× → 推出"发射白天看不见"
  (发射就在 14:00,荒谬)→ 绑账 4,昼 SNR 实为 567;②标定幅度比账 9 低 2500× → 绑账 9;
  ③账 9 注入 10% 反馈满量程(1e-2)会把 VEL 科学档(±2e-3)打饱和 → 定版注 VEL 档 5%,
  "标定信号必须留在被标定的通道里"。
- **中 ×3**:标定窗 6.7% 概率盖住 14:00 发射时刻(JS 已改发射优先);quiet 卡"B 均过线"
  实为差 8 m 恰在线内(卡已改:B 余量为零,矿场是 B 案第一风险项);屏"300×"重算为 ~2000×。
- **低 ×2**:PV 移出 nightMats(面板不是灯);sim/10 docstring OSR 512→256。
- **另记一条落位注意**:方位盘的发射红标与 JS 硬编码 56° 假定了资产朝向,与指向牌同理,
  总控定 rotation_deg 时两者一起校准(改 azMark(56) 与 launch baz 常数即可)。
- 结构性教训(本项目二次验证):**高严重度错误全部长在两本账对同一个量各说各话的地方**,
  跨账一致性闸是抓错的第一工具;凡共享物理量,一律闸绑定。

## 落位确认与收尾轮(08-09)
- 总控已落位候选 A:(-700,-520) sink 0.32(采纳交接建议 0.30~0.35),audit clean。
  评审后回归在**正式落位状态**通过:scale=1、38 资产、nightMats=1(PV 已移出)、
  标定窗 8 块全琥珀、发射窗内标定被正确压制(0 琥珀+屏满栅)、console 零报错。
- 城内互引已经活了:sci-fel-01(08-09 交付)选址引用本站 400 m 红线(实取 625 m)、
  振动账引用城市隔振线 1.2e-6 m/s/√Hz@10 m 并反向审计其在本站处的振幅
  (仅矿场合规值的 1.6e-3)——账 3 的反向输出成为城市规范的实例 +1。
- 坑账新增:upload_server 收图中途对帧目录做批量 mv 重命名会把服务打死(POST 打开
  半路被改名的路径)——**先停拍再改名,或从一开始就用足位数编号**。

## Git 勘误(08-09,报总控)
mars 仓有两笔**提交信息与内容不符**的提交,系本 session 链式命令 cwd 继承事故
(第二行 `git add -A` 继承了第一行的 `cd /e/Claude/mars`):
- `3b2869f "Ledger 11: end-to-end..."` —— 实际内容是当时 mars 工作区里并行 session 的
  未提交文件(ir/swir/thz 资产、HANDOFF 等),不是火震设计册;
- `1f96d0c "Review round: amplitude binding..."` —— 实际内容是 cryo/eclss/fel/ir 等
  并行文件与 manifest 重排。
两笔的**文件内容本身无损且是各 session 迟早要入库的交付物**,历史其上已叠多笔提交,
故不重写;真正的设计册内容已补提交到 mars-seis 仓(`c554f92`)。若总控要澄清历史,
可加 git notes 或在各自 CHECKLIST 行注明真实提交者。
**本 session 三次 git 事故同根**:多仓并行 + 链式命令 + `git add -A`/整索引 commit。
纪律定版:共享仓一律 `git -C <repo> add <点名文件>`,永不 add -A,提交前必看 status 首列。
另:mars-seis/shots/ 出现 5 张他人截图(c1_overview/c2_shaft/c3_screen/c4_atmos/c5_full,
疑似某 session 收图服务误指向本仓),已移入 shots/_foreign/ 未删,认领请自取。
