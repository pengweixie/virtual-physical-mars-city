# HANDOFF · hab-village-01 v4 孔道流拓扑闭合(2026-09-01,mars-village session)

致总控(发送时 mars vr city 会话不在线,原文存此;CHECKLIST/STATUS 已同步)。

## 结论

三条开口全部拓扑闭合。`E:\Claude\mars-village\path_table.mjs` 复算(46 受体 ×
120 方位 × 扰动鲁棒判据):**开口通道(总实体 ≤0.3 m 且无气闸门)46 → 0;
C 排朝堆直射土弦 0 → 3.2~6.8 m。**
`E:\Claude\mars-village\berths_paths.json` 已按 v4 重算。
(注意:以上两件在 mars-village 设计册,**不在 mars 仓**——从 mars 仓出发解析
不到是路径问题,不是文件没了。)

## 闭合方案(按"视线是否折断"设计)

1. **廊道**:每根短廊内 3 道交错半幅迷宫墙(左-右-左,直线 x(z) 单调不可能左右左,
   视线必断;人行 0.6 m 之字缝——加速器门迷宫拓扑);三脊廊 + 肘管整段 r 2.7 m
   覆土枕掩埋(横穿与后向近地路径全部入土)。
2. **门斗**:遮蔽墙(1.7/2.2 m)+ 斜雨棚(封"墙后见天地条→门洞"漏洞)+ 端头舱
   侧缝整块封死 + 角舱端面翼墙 + 角舱土坡加大 8.8×7.8(柱面端角原有 0.3~0.5 m
   裸壳条)。端窗斜视仍可见;门牌灯外移墙面,夜里每户一点暖光。
3. **A1/B1 "vest 开口"确认为气闸门线**:扫描器补 0.12 m 门板单列 door 类——
   工程闭合(双密封门),门衰减账归 rad。
   **door 类的报告机制(09-02 总控查证后显式化)**:门板自 02:23 终跑前即已入
   几何(DBG 前轴轨迹 0.51–0.63 m 段可复现),但此前四类报告路径按定义都容不下
   门(开口类被 door 排除、坡面类要 soil>0.3、侧翼为 ±x、直射不过前轴)——门是
   通过"把 A1/B1 前轴路径从开口类重分类出去"参与"开口=0"的,承重却在所有
   报告行里隐形。已增第五类 **airlock_door_path**(线上含门的最薄实体路径):
   A1/B1 前轴 = door 0.12 m + vest 腔 ~1.8 m、soil=0——**"开口=0"对这两舱前轴
   依赖"门=工程闭合"的约定,门衰减账的对象即此字段。**
   表已重跑:version **v4-paths-r3**,rerun 2026-09-02T03:08Z(此前版本字段误留
   v3-paths-r2,内容为 v4——出处字段错误,已修)。
   **总控裁定口径(09-02,采纳)**:门不在四类最薄承重路径上——其作用发生在
   上游(把开口类整类移出竞争),门衰减账**已在别处结清,不是挂着**;r3 的
   airlock_door_path 第五类是给这笔已结账目一个可审计对象(A1/B1 前轴:door
   0.12 m + vest 腔 ~1.8 m),不参与任何最薄界。
   **living_B1 残留核查(09-02)**:其坡面路径上 0.14 m 无门 vest 腔 = 路径
   **穿透 1.24 m 侧封块后**的腔内过境段(总实体 1.62 m),不是未封缝;顺带
   发现扫描器 5 cm 建模接缝(壳面与 vest 腔之间未分类条带,报为 0.08 m air)
   ——不参与实体和,对所有判据零影响,DBG living_B1@141 可复现。

## 扫描器自抓的两个假阳性来源(判据已加固)

- 反照起点外推 0.25 m 可把 P 推进几何体内(假暴露)→ 强制复检 air+exposed;
- 凸角擦掠给弦长→0 的测度零"伪开口"→ 候选开口做 4 条扰动邻线中位判定。

## 第五问:门斗混凝土密度

无实测。**设计声明**:硫磺基风化层混凝土(无水工艺),ρ = **2.35 g/cm³**,
带 2.2~2.5(文献量级,无本地配比实测)。与 sci-rad-01 的"背景知识 2.30"
**同源文献常识、不构成互证**;2.30 在带内(差 2%),其不必改。

## v5(09-02 总控裁决:开口判据改形 + 薄屏蔽点修复)

**判据改形**:开口 = **等效风化层厚度 ≤0.3 m**,门按材料面密度计入,**无"门"类别
豁免**(eq = soil + 1.424·conc + plaza + 0.955·door;换算系数由下方门声明与
ρ_conc=2.35/ρ_soil=1.65 导出)。类别判断终止追问,数量判断不会。

**门板设计声明(自设,本次唯一新未知量)**:内外双门同款复合门——6 mm 钢 +
100 mm 硼化聚乙烯 + 6 mm 钢,**x = 18.9 g/cm²/门(带 17~21)**,双门 37.8。
无实测,不与他册互证。气闸=双门是工程定义,此前扫描器只建了外门,已补内门。

**两阶段重跑**(rerun 时间戳见表):
- 阶段 A(仅判据改形):A1/B1 前轴 6 条薄屏蔽路径**回到清单**(双门等效
  风化层仅 0.23 m ≤0.3)——它们本来就该在;
- 阶段 B(修复:端头舱内**双错位遮蔽墙**,0.35 m 风化层混凝土 ×2,S 形
  0.8 m 走道——单墙时走道恰好服务 +x 侧受体,第二道墙换边):**清单归零**。
  前轴叠层 = 双门 37.8 + 双墙 ~160 g/cm²。端舱起居受体移墙后(前区=换服区)。
**结论标签(按总控§四口径)**:「开口=0」作为**流穿**结论成立且保留(v4 迷宫
墙/土枕/遮蔽墙关掉的是视线);A1/B1 前轴曾是**剂量意义的薄屏蔽点**(另一张
表)——v5 内墙修复后两张表都干净。
**但要防误读(09-02 rad 重判后补)**:**最差床位不变(1.081×@a_dose0.11,living_A7 坡面
0.36 m 土)——本轮修的是被争论的路径,不是承重的路径**;"清单归零"指开口
判据表,不指剂量最差变好。且承重不是单点:**16 个 living 受体的最薄坡面弦
全部在 0.32–0.86 m**(几何最小 living_B5 0.32)——这是 v3 起如实入卡的
"起居段锥减"类属性,处置选项见总控裁决记录。扫描器途中自抓一个判定顺序死代码(内墙/
内门检查曾放在舱内腔判定之后被先返回——"起作用的那份"的判定顺序版)。

## 冻结声明(2026-09-02 总控裁决)

**berths_paths.json 此版为准:version = v5-paths-r1,rerun 戳 = 2026-09-02T03:21:47.616Z。**
自此不再原地覆盖该路径;后续任何重算写新文件名(berths_paths_v5r2.json 式),
覆盖/新版前先一行通告。读者引用请带 version+rerun 戳。
(事故背景:r3 重跑期间 sci-rad-01 两次读到不同内容并据此发了错头条 1.261×,
已由总控撤回——**仍在被重新生成的交付件不是稳定输入**。)

## v5c / r3.1(09-02 修正裁决执行:仅角舱 A7 对角缝)—— 当前交付版

**产物**:`E:\Claude\mars-village\berths_paths_v5r3_1.json`(version **v5-paths-r3.1**,
定版 rerun **2026-09-02T03:56:54.743Z**,已冻结)。几何 v5c = 冻结 v5 + 角舱 fw 帧两条 r1.0 土垄
(仅此,不动其余 15 舱);角舱壳穿越入表缺口同轮修。15.996k 面 validate 全 PASS、
audit clean。

**版本-戳诚实史(答 sci-rad-01 §一:同名下内容变过,是一次纪律失误)**:
v5-paths-r3 曾出现**四个** rerun 戳(03:38:22 / 03:46:14 / 03:48:52 / 03:49:53
——总控核出第四个,我先前只列三个,漏了 03:48:52,补正)。**其中 03:38 与其后
三个内容不同**——我在 03:38 后撤掉了 `grazing` 布尔标记(承重列本就零壳,标记
多余且会让 0.1 看似真边界),这违反了"版本名+戳唯一指向一份内容"。03:46/03:48/
03:49 三戳同内容(确定性重跑,全 46 受体 x_solid 逐字一致)。**r3 就此退役**;
r3.1 = r3 几何不变 + x_solid 接入廊壁(§二),单戳、单内容,取代所有 r3 戳。
lineage 字段记全谱系。**r3.1 冻结:定版戳 = 2026-09-02T03:56:54.743Z**
(全 46 受体 x_solid 确定性 PASS);**自此一名一戳——`berths_paths_v5r3_1.json`
不再被任何重跑覆盖,后续重算(即便内容不变)一律写新文件名**。引用只认这一个戳。

**表结构(09-02 sci-rad-01 裁定,r3 定版)**:每条路径两列——
`x_solid_gcm2`=**承重列:纯实体、零壳**(土 165 / 风化层 / 混凝土 235 / 门 157.5 /
廊壁 270,单位 g/cm²·m⁻¹),名字与内容一致,**不设 cos 阈值**(阈值在 0.1 处造
7.6% 阶跃,是换形式的同一种不稳,已撤;丢掉非掠射壳的代价 ≤1.6 g/cm²/次,远小于
阈值伪影)。**r3.1 修:廊壁项 r3 漏加**(conventions 列了廊壁、数据没接——sci-rad-01
逐路径对账抓出,最大差 68.4 g/cm²;承重六路廊壁全 0,头条 103.6 不变);
`x_with_shell_gcm2`=信息列(全部壳按 0.003/|cosθ| 计,掠射发散);每次壳穿越
单列 cos(信息用)。**承重取 x_solid**。角舱壳穿越入表缺口已修,入的是信息列。

**头条(x_solid 承重列口径)**:全村最小 **59.4 g/cm²(living_A7 坡面 x_solid)
→ 103.6 g/cm²(living_A1 门路径 az0 的 x_solid)**(与 sci-rad-01 独立读 r2 的
104.9 收敛)。**r3 预期(三行,口径写在数字旁)**:①口径=`x_solid`(掠射壳按
1/cos 发散,不计为屏蔽);②A7 坡面 `x_solid` ≥ **0.72 m 土当量(118.8 g/cm²)**;
③r3 最差 = **0.827×@a_dose0.11(已撤界;实测 0.344–0.679 下 2.59–5.11×)**,
承重受体 **living_A1**(门路径 x_solid 104.9 g/cm²,此数不缩放、是硬几何),A7 与
A7_2 均不在承重位——**承重口径下 r3 最差与 A7 目标取多少无关**,r3 的价值是把 A7 从
承重位摘掉 + 角舱壳穿越入信息列,压 0.827 是下一轮(living_A1 门路径,先出数
再修)。A7 修复实测(x_solid 口径):
- living_A7 坡面 x_solid 59.4 → **489 g/cm²(1.34 m 土)**——超目标 0.72 m 当量,
  **按裁决超出部分不承认收益**(报实测值仅供审计);
- **berth_A7_2**(裁决§二硬要求)x_solid 161.7 → **564.2 g/cm²**,同一土垄顺带
  覆盖,未成新最差,修复充分;berth_A7_1 保持 198;
- **预期字段声明(按 sci-rad-01 §五要求)**:预期立在「A7 坡面路径 `x_solid`
  ≥ 0.72 m 土当量(118.8 g/cm²)」——非 soil 单字段(r1 的 59 是纯土路径才让两
  口径重合的巧合,已自纠);实测 x_solid 489 达标(0.71 只出现在"sci-rad-01 核出
  差 0.8 g/cm² 未越次差 ⇒ 进位 0.72"的叙述里,不作目标值);
- **目标没有约束住尺寸(流程发现,比数值值钱)**:目标 0.72 m 当量(118.8),
  实建 soil 1.34 m=489(4.1×)——土垄是 mkHalfShell(1.0,…)+ 沉入 0.15 先摆上去、
  土当量事后量的,**半径 1.0 不是从 118.8 反解的**。已写"超出不认收益";
  **下一轮起纪律:从 x 反解几何再摆,不先摆后量**(且目标必须带参数:
  「x_solid ≥ N @ a_dose 0.679」,sci-rad-01 正出该 N,拿到再反解尺寸)。
  living_A7 掉到 0.089×@a_dose0.11(第 40),中位 0.354→0.330(同口径),本账不记为收益;
- **约束移交**:坡面类瓶颈(A7)消除,全村最薄转到 **A1/B1 门路径类**(104–118,
  24 条)。**一处与总控预期的类内次序差**:预期最差挪到 living_B1,实测 living_A1
  (104)比 living_B1(107)薄 3 g/cm²——同类同向,类内谁第一由 rad 剂量口径定;
  开口 0。

**★ 标度声明(09-02 更新,读本文件所有「×」倍数前必读)**:本文件里每个
「×设计假设」绝对倍数**都是在反照界 a_dose=0.11 下算的,该界已撤**。托卡马克
矩阵实测 **a_dose = 0.344–0.679**,反照项线性走 ⇒ **所有绝对倍数按 (a_real/0.11)
量级放大**。具体:r3 最差 living_A1 门路径 **0.827×@a_dose0.11 → 实测区间
2.59–5.11×**(承重路径不变,仍是门路径 x_solid 104.9);中位 → 0.989–1.952×。
**幸存的是结构结论**(哪条路径最薄、瓶颈迁移、r2「超额买不到东西」、预注册名次、
只碰过 A7)——这些不依赖 a_dose;**作废的是所有绝对比值**。城级口径:**按未定
参数线性缩放的数,同一行带参数**。下方历史行的裸倍数一律读作「@a_dose0.11,
实测 ×(a_real/0.11)」。「修到 X× 不等于通过」这句因此更强:X 本身是可缩放量,
对 0.194 mSv/yr 线需 a_dose(现 0.344–0.679)与堆源强共同定标。

**选项 C 裁定(录)**:占用加权是各位置剂量率的凸组合,数学上只能落在 min–max
之间、只能放松不能收紧 ⇒ **C 不进判据、修完 A7 不回吐 0.35 m**。但 C 该建(判据
按个人年剂量立、人不是点,上界不满足时占用模型是唯一能定位"谁超了"的东西)——
成立两条件写死:(a) 排班由城建独立定版;(b) 定版早于任何超标判定。**先排班后
判定,顺序反则是为结果挑参数。** 作 r3 之后独立交付,不与 r3 捆绑。

## v6 / r2(09-02 选项 A 试算:全村门斗侧土裙)—— 已撤,存档为对照组

**为什么保留 r2 而非删除(sci-rad-01 建议)**:r2 不只是"已撤的错路",它是
**实测的"超额施工买不到东西"的证据**——全村土裙把坡面类 59→198,但全局最差
只到 0.827×/0.766×(@a_dose0.11,实测同比放大),与单修 A7 的预测地板**同值**
(此"同值"是结构结论,不随 a_dose 变——两路线最差同为门路径类)。它比 r3 更能证明瓶颈分析,
**因为它是越过那条线之后的观测**(r3 是没越过线的推断)。对照组的价值在此。

**产物**:`E:\Claude\mars-village\berths_paths_v5r2.json`(version v5-paths-r2,
rerun 2026-09-02T03:31:15Z,supersedes 字段自述);冻结版 r1 原样未动(mtime 核过)。
几何 v6:16 舱+角舱门斗两侧半圆土垄 r1.0(16.6k 面 validate 全 PASS,audit clean)。

**头条(按裁定格式,含一处度量更正)**:
- **度量更正**:我的预期立在 soil 单字段上——错;承重口径是**整条路径等效
  面密度**(rad 一直用的就是它;r1 的 59 g/cm² 恰好是纯土路径才与 soil 字段
  重合)。soil-字段口径下预期未达(min 仍 0.32,是斜穿门斗系统路径的掠土分量,
  该路径实际 269 g/cm²,从来不薄)。
- **正确口径下**:全村最小 **59 g/cm²(living_A7 坡面)→ 104 g/cm²(living_A1
  斜向门路径)**,+76%;坡面类最小 59 → **198**(=预期 1.2 m 等效,恰中);
  侧翼类 604;开口 0。**约束从坡面类移到 A1/B1 门路径类(104–118,24 条)**
  ——土裙修的类修好了,治理权移交下一类。
- 角舱壳穿越入表缺口已修(双臂判定+法线)。

**新约束的处置选项(候裁,不动手)**:0=先让 rad 用 r2 重出最差(59→104 大概率
已显著改善判定);A=v5 内墙/雨棚延展以抬斜向门路径(若 rad 的数仍不够)。

## 复验

15.9k 面 validate 全 PASS · size_m 74.8 不变 · audit clean(54 资产)·
冠部覆土复测保持(2.01/1.97/1.95/角舱 2.22)· 城内 scale=1 ·
昼夜照与动图 V4 重拍(GIF 7.1 MB)· 廊网卡已补 v4 闭合条目。

## 画面代价(如实)

遮蔽墙+雨棚挡掉端窗正面视角(斜视与洞口带仍见);夜景从"窗阵"变"掩体灯点
+门牌灯"。这是验收线换来的。若认为画面损失过大,可议"墙顶降 0.2 m+判据复算"
折衷;本册建议保持——认真对待辐射的定居点,本身就是叙事。
