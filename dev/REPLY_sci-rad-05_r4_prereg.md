# PREREG — sci-rad-05 r4 · r3 留下的两处,预期与闸写在任何数之前

**册子**:`E:\Claude\mars-rad-telescope`,r3 交付 `1481eb0`(账 `59b4cf8`,预注册 `3313c85`)。本文在 r4 任何脚本跑出数字之前写下并提交,提交号见本文所在提交。
**输入**:只用 r1–r3 已落库的 `out/*.json` 与 `sim/*.py`;三份 DISPATCH 至今(2026-09-17)无回执,不引任何回执;不引科学城裁定。
**纪律同 r1–r3**:预期不改;偏出照记;闸带子不动;必须红的支若不红,先证明闸能红或前提已被证伪,再按名排除并附理由;INEFFECTIVE 是失败。r1–r3 的 json 一个字节不动;r4 另立 `out/13_*.json`、`out/14_*.json`、`out/15_*.json`。

## 0. 起点(r3 已落库的数,`out/10_field_mapping.json`,不是预期)

| 项 | r3 的数(1.2 / 1.44 / 1.6 族) | r3 写下的说法 |
|---|---|---|
| A. 主望远镜停止类剂量对真值 | 对 1.5 cm 片 1.17 / 1.16 / 1.09;对 15 cm 宽片 1.23 / 1.18 / 1.14;真源分解:族内 0.84 / 0.84 / 0.87,氦 0.147 / 0.145 / 0.114,族外能段(>出射能)0.017 / 0.016 / 0.018 | DELIVERY r3 写「内圈分段类没有这个(账 01)」「量得出的族部分该只用内圈类」 |
| B. 穿透 Z=1 类剂量对宽片真值 | 0.82 / 0.83 / 0.83 | 「z1 判据(中值 <0.30 MeV)与响应的联合标定」 |
| C. 量得出的族部分 / 区间 | 1.03 / 1.08 / 1.13(偏 +0.005 / +0.009 / +0.023);[1.04, 2.26] / [1.09, 2.17] / [1.13, 2.07] | — |

**先登记一条对 r3 说法的怀疑(在算之前):** 「内圈分段类没有氦串扰」这句话的出处是账 01 的混淆矩阵,而账 01 的标定样本只铺在停止能段(10–95 MeV/n)里。开 r4 前重读 `telescope.transport`:核作用在本册模型里**就地结束径迹、不触发反符合 F**。高于出射能的相对论氦在 300 µm 硅里沉积 4 × 最小电离 ≈ 0.4–0.7 MeV,与 40–110 MeV/n 停止质子的 ΔE 相同;它在 D(3 cm CsI)里核作用后留下一个任意的 E,拟合就在 p/d/t 三条线里找到一条。这个机理与角度无关,内圈分段解不了它。氦→Z=1 的质量台阶是 m z² 的 5 倍以上,路径长 1/cosθ ≤ 1.25 换不出来——所以「全接受度下角度简并」这个 r3 的归因在量级上就不成立。下面的预期按这个判断写;若数据说内圈类确实干净,那是我这条判断的偏出,照记。

## 1. 账 13 主望远镜停止类的氦闯入:机理、内圈类、旁带扣除 `sim/13_he_intruders.py` → `out/13_he_intruders.json`

方法(先写死):
- **13a 真源分解**:账 10 的 `main_class_sources` 同法,分别作用在全接受度类与内圈分段类(`ev['inner']`)上,三个族;对氦闯入者另印:真 T/A 是否高于氦出射能、`interacted` 标志、停止层。
- **13b 内圈类路径(r3 承诺的那条)**:给内圈类建自己的响应表(账 02 `response()` 同法,只数 `inner` 事件),量得出的主望远镜部分只用内圈类;与全接受度路径并印,附每 sol 统计误差。
- **13c 旁带扣除**:仪器自己量得到穿透 Z=2 类。用氦标定束(出射能以上,对数平坦)建转移表 r(三层硅中值 ΔE 箱 → 判为 p/d/t 的停止类的 (种类, T/A 箱)),即「每一个按 Z2 穿透类计到的氦,对应多少个漏进 Z=1 停止类的氦」;用场里**测得的**穿透 Z2 类中值直方图乘转移表,得到闯入强度的预测,按同一映射从主望远镜部分的 D 与 H 里扣掉。高于出射能的质子闯入(0.017)不扣,点名留着。
- **13d 失配注入**:转移表在标称核作用长度下标定;场样本另跑两份,氦在 CsI 与 PVT 里的 λ_int × 0.77 与 × 1.43(截面 ×1.3 / ×0.7)。扣除后残余。

预期:
- E13.1 内圈类的氦份额(强度权重)**0.08–0.16**,三个族都是(全接受度类 0.11–0.15)——即 r3 那句话**预期被证伪**;氦闯入者按权重 **≥ 80%** 真 T/A 高于出射能且 `interacted` 为真。
- E13.2 内圈类路径的主望远镜类剂量对宽片真值 **1.08–1.25**(没修好);每 sol 统计误差是全接受度路径的 **3–4.5 倍**(几何因子 0.18 对 2.47)。
- E13.3 旁带扣除后,全接受度类剂量对宽片真值 **0.97–1.08**(r3 1.14–1.23;剩下的是不扣的质子闯入 ~2% 与映射闭合差 ~2%)。
- E13.4 失配:截面 ±30% 下,扣除后比值仍在 **0.93–1.12**;残余氦份额 **≤ 0.06**。
- 闸:
  - G13.1 转移表闭合:在一份**谱不同**的独立氦样本(出射能以上 E^-1.5,不是对数平坦)上,预测闯入剂量对实际闯入剂量 **±15%**。
  - G13.2 旋钮到达数字(账 11 首版的教训):λ_int × 0.77 时,氦在 D 里的 `interacted` 比例相对标称上升 **≥ 15%**;不满足则 13d 整节 INEFFECTIVE。
  - G13.3 **必须红的支**:用关掉核作用(`no_interactions=True`)的氦标定束建转移表,预测的闯入必须 **< 标称预测的 10%**——证明闯入走的就是核作用这条道,而不是别的。
  - G13.4 扣除后每族主望远镜类氦份额的**预测值**对真源分解的实际值 **±25%**。

## 2. 账 14 穿透 Z=1 类:z1 判据与响应的联合标定 `sim/14_pen_z1_joint.py` → `out/14_pen_z1.json`

算之前写下的机理判断:113 MeV(出射能)的质子在 300 µm 硅里沉积 0.39 MeV,降到 0.30 MeV 要到约 165 MeV;所以 113–165 MeV 的穿透质子被「中值 <0.30」判进了 Z2 组,而这一段单粒子在参考硅片里的沉积最大。响应表按整段平均效率补了**个数**,补不了**剂量权重**。另一侧,相对论氦的三层中值约 0.36–0.45 MeV,与这段质子重叠,单靠硅分不开;E 层(2 cm 塑闪)分得开:将出射的质子在 E 里沉积 20–45 MeV,相对论氦约 16 MeV 且 D 层 ~70 MeV 对质子的 ~100 MeV。

方法(先写死):
- **14a 缺口分解**:宽片真值的穿透 Z=1 类剂量,按真能段(113–165 / >165 MeV/n)与种类(p / d+t)拆;每一块被现行 z1 判据收进了多少。
- **14b 修订判据**:Z=1 当且仅当 中值 <0.30,**或**(0.30 ≤ 中值 <0.60 且 (D, E) 两层沉积在对数空间里离质子假设的预测比离氦假设的近);预测来自同一张阻止本领表的正入射 CSDA(仪器自己的表)。
- **14c 连续响应**:Z=1 穿透类的 G_eff 按中值 ΔE 分箱(质子标定束,出射能的 1.0–30 倍,对数平坦),不再是一个数。
- 映射不变(硅片沉积 = 中值 × cos_inner × 2,Q = 1;声明)。

预期:
- E14.1 缺掉的 0.17–0.18 里 **≥ 60%** 是真 Z=1、中值 ≥0.30 的那一段(113–165 MeV/n);d+t 占穿透类真值剂量 **≤ 10%**。
- E14.2 修订后类剂量对宽片真值 **0.93–1.05**,三个族都是。
- E14.3 修订类里的氦份额(剂量权重)**≤ 0.03**。
- 闸:
  - G14.1 响应闭合:独立质子样本(出射能以上 E^-1.5)上,修订链回收的类剂量对该样本的映射真值 **±6%**。
  - G14.2 **必须红的支**:去掉 (D, E) 条件、只把中值判据放宽到 <0.60,氦份额必须 **> 0.10**——证明分开它们的是 E 层,不是运气。
  - G14.3 修订判据不动 <0.30 那一段已收的事件(恒等:旧类是新类的子集)。

## 3. 账 15 r4 的族 ⟨Q⟩ 汇总 `sim/15_family_r4.py` → `out/15_family_r4.json`

方法:账 10 的链,主望远镜部分换成账 13c(全接受度类 + 旁带扣除),穿透部分换成账 14;低能段部分、反冲项、亚阈项不变。r3 的数并印。

- E15.1 量得出的族部分对自身真值 **|Δ| ≤ 0.03**,三个族(算之前的算术:扣氦动 D 约 −0.014、Q=1,对族 Q 的作用 ~+0.002;穿透类补 ~+0.07 的 Q=1 剂量,作用 ~−0.011)。
- E15.2 区间:下限**下移 ≤ 0.03**,上限**下移 0.05–0.15**(量得出的 D 变大,反冲项相对变小);三个族仍包住场值。
- E15.3 2.5 仍在三个族的区间外;2.0 对 1.2 / 1.44 族仍在区间内(对 1.6 族 r3 上限 2.07,r4 **可能**掉到 2.0 以下——这条不写预期,只写「会印出来」)。
- E15.4 量得出的部分每 sol 统计误差 **≤ 0.03**(扣除引入的方差计入)。
- 闸:G15.1 三个族包住场值(与 G3.5 / G10.3 同);G15.2 部件恒等:低能段部分与账 10 逐位相同(import,不重算出别的数)。

## 4. 资产与渠道

- **r1 的一条交付顺序缺陷(登记)**:ops-compute-01 与 ops-polymer-01 的 r1 烟测(2026-09-13 11:03–11:07)各记了一条 `viewer/units/sci-rad-05.info.json` 404——我的模块 10:50 先到城仓,卡后到。文件自 r1 交付起已在(现 sha256 前 16 位 `bafa44d16b72a6c8`)。r4 起同一单元的模块、卡、manifest 条目按「卡 → 模块 → manifest」的顺序落,manifest 最后。
- **动图**:r2/r3 两种拍法都在无头 Edge 里,得到静止帧。r4 只试一种**机理不同**的拍法:在浏览器面板里沿用夜景截图的泵帧法——同一个 JS tick 里 `animate(t)` → `renderer.render` → `canvas.toDataURL`,t 按 1/12 s 步进 36 帧,逐帧 POST 到本地收图口,用 PIL 离线拼 GIF。验收:相邻抽帧的变化像素数 > 0,且 ΔE–E 牌的事件闪点位置随帧变。不达标记「未交付」,不试第二种。
- 卡:8 卡不增不减;质子族牌与剖面牌追加 r4 条目,被取代的值原地标注。三份 DISPATCH 按 r4 的数更新;「内圈类没有氦串扰」若被证伪,进 `check_retired.py` 并在 DISPATCH_to_sci-rad-01 §5 原地标注。

## 5. 交叉矩阵与不为填空造参数

- 账 13/14/15 共用账 02 的 `_calib_run` / `classify` / `response`、账 10 的 `field_map_events` / `truth_parts_wide` / `interval_from`(import,不复制)。
- 不能闭合候选(先点名):本册的核作用模型是「就地结束、不出次级」的声明粗模型。真实仪器里核作用的次级有相当一部分会打到反符合 F 上,闯入率会比本册低;低多少,需要 (a) 氦在 CsI 里的碎裂次级多重数与角分布的测量或输运码结果,(b) F 的阈值。本册两样都没有,**不造**一个「F 否决效率」参数;账 13 给的是「无次级否决」这个保守端上的数,写明。

## 6. 记分与提交

每本账 json 写 `scorecard` 与 `gates`;`sim/run_all.py` 纳入 13/14/15;提交经 `tools/commit_gated.sh`。

Produced by: sci-rad-05 session。本文是预注册,不含任何 r4 算出来的数。
