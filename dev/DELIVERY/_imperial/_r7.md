# 天宫城 R7 — 主城文件通道交付

交付日期：2026-09-05。目标：`E:\Claude\mars`。来源：`E:\Claude\mars-imperial` 的 Astra R7，本地提交 `92b8765`；本次另含主城坐标、真实地形桥体与知识卡适配。

**状态：文件已落位，天宫城专用 validate 与渲染预算通过；主城第一人称碰撞、知识卡 UI 接线仍待总控。不是“进城体验全部验收通过”。** 本交付不发送任务消息，不提交或推送主城仓库；由总控合并本轮文件。未修改 `viewer/main.js`、manifest、地形数据或既有 GLB。执行期间其他工作流出现的村落文件改动均未触碰。

## 1. 落位清单

| 文件 | 用途 |
| --- | --- |
| `viewer/imperial/imperial-city.js` | 单文件图层模块；`build(ctx)`；`visualRevision=7`、`deliveryRevision=r7-main` |
| `viewer/imperial/imperial-city.info.json` | 双语知识卡，5 个 POI；与模块 `poi_` 空锚点逐一对应 |
| `dev/DELIVERY/_imperial/validate-r7.mjs` | 本次专用 CPU 验证器，读取主城真实高度场并检查实际网格 |
| `dev/DELIVERY/_imperial/validate-r7.json` | 验证结果、Y 包络、三角形、每 50 m 检查点与输入哈希 |
| `dev/DELIVERY/_imperial/smoke-r7.html` | iframe 加载未改动的主城，日景／夜景／图层预算／碰撞检查按钮 |
| `dev/DELIVERY/_imperial/browser-smoke-r7.json` | 实际主城 WebGL 加载、GLB、draws、fps、碰撞与控制台记录 |
| `dev/DELIVERY/_imperial/validate-units.log` | 当前全库通用校验的原始输出（退出码 1，原因见下） |
| `dev/DELIVERY/_imperial/main-day-axis.png` | 主城真实地形、下午 15:00、白天轴视 |
| `dev/DELIVERY/_imperial/main-day-oblique.png` | 主城真实地形、白天斜视 |
| `dev/DELIVERY/_imperial/layer-day.png` | 隔离天宫城、诊断日光、预算面板 |
| `dev/DELIVERY/_imperial/layer-night.png` | 隔离天宫城、诊断夜光、预算面板 |
| `dev/DELIVERY/_imperial/manifest-r7.json` | 本次文件 SHA-256 清单；不包含清单自身 |

知识卡路径沿用代码模块同目录 sidecar 约定；模块通过 `group.userData.infoUrl` 暴露 URL，但不会自行创建 UI。

## 2. 改动清单

### Astra R7 本体（保留此前骨架与塔楼族）

- 主殿重檐堆叠、屋身高度与台基比例细化；三重檐、三层各 2.5 m 白石台基保留。主殿台基南侧栏杆开口 96 m，补实体抵达平台。
- 最上层台地向北延展，主殿北移，为最后一段台阶留出真实空间；不向中轴添加建筑或器物。
- 御道主阶名义踏步高改为约 0.18 m、踏面 0.54 m；桥头细踏步、抵达平台与台阶实际表面连接。
- 深色铺地改平面；瓦垄剖分与栏杆重复几何减面。树保留十字面片，不恢复逐瓦／逐栏板雕刻。
- 删除水面的假金色矩形倒影，改为真实金顶几何的一次性 128 px 立方环境捕获及 PMREM 预过滤；水 roughness=0.15。这是近似环境反射，不是逐池平面反射。

### 本次主城适配

- 中轴射线使用世界坐标发射、局部坐标回写，适配主城已有整体平移 `[1100,0,-800]`。
- 金顶反射探针也应用图层世界矩阵；本次探针世界位置实测 `[1100,58.5828,-658]`。日光环境反射随引擎太阳高度在夜间衰减。
- 修复真实地形下的桥体问题：桥基改采样河道所在位置；拱洞高度限制在桥体之内。原初交付候选在桥北端实测有 2.274 m 高差，修复后无此断面。金水河高度同样使用河道采样，不再套用城中心高度。
- GLB URL 相对模块地址解析，预览入口和主城入口均指向既有 `models/imperial/{lion,huabiao,censer,turtle,crane}.glb`，失败仍静默保留占位。
- 补齐 5 张中英知识卡及 `poi_imperial-{bridge,axis,terraces,summit,bell-drum}` 空锚点，不增加面数和 draw call。

## 3. validate / 三角形 / Y 包络

复现（PowerShell；无须改动 package.json）：

```powershell
Set-Location E:\Claude\mars
node --check viewer/imperial/imperial-city.js
node dev/DELIVERY/_imperial/validate-r7.mjs
git diff --check -- viewer/imperial/imperial-city.js
node scripts/validate_units.mjs
```

前 3 项及限定文件的 diff 检查通过；全库 `validate_units.mjs` 不通过，详见下一节。

专用验证器统计 **所有程序化实例**，不以视锥内可见面数代替总量；包含未装载 Rodin 时的占位几何，不含 Rodin GLB。

| 验证场景 | 程序化三角形 | 全几何世界 Y 包络（m） | 60 s 动画采样后的全几何 Y 包络（m） |
| --- | ---: | --- | --- |
| 平地、原点、sampleHeight=0 | 168,400 | 0.000000 ～ 185.100220 | 0.000000 ～ 185.100220 |
| 主城真实地形、既有站位 | **169,640 / 170,000** | **28.662101 ～ 228.507462** | **28.662101 ～ 228.507462** |

预算余量仅 **360 面**；换站位会改变台阶数，不能将当前数字套用到任意地形。没有为了“minY≈0”把真实地形高度减掉或抬动主城图层。

主城实际全几何包络（不含 Rodin）：

- X：509.600006 ～ 1690.399994 m；Z：-1281 ～ -328 m。
- 包络尺寸：**1180.799988 ×199.845361 ×953 m（X×Y×Z）**。953 m 包括南端阙楼和 R7 北移后的顶层，已超出最初约 800 m 深度；不以早期设计包络替代实测值。
- 烟雾单独 60 s 包络：Y=110.197760 ～ 147.256507 m；采样 dt=0.1 s，共 601 帧；所有坐标有限。该结果为采样验证，不是无限时间解析上界。
- 107 个程序化 Mesh、8 个点光源、2 个动画回调、3 个 nightMats；未发现禁止的光源或不符合约定的材质类型。
- 主殿仍三重檐；塔楼族记录的最高相对高度比 0.618997，低于 0.75。
- 路径长度 778.41 m，157 个 5 m 采样点、17 个含终点的 50 m 检查点。真实三角形每 0.1 m 射线检查共 7,785 处：断面 0、路径高度不匹配 0、地形高于中轴表面 0；最大相邻高度变化 0.229283 m（包含铺地叠层）。这不是主城碰撞器结果。
- 5/5 知识卡中英字段和命名锚点验证通过。

### 全库通用 validate 的明确边界

`scripts/validate_units.mjs` 从 manifest 读取普通 `build(THREE)` 资产，**不包含 `build(ctx)` 的天宫城图层**。本次实跑退出码 1，五个失败均为其普通资产的 minY 检查：

| 资产 | minY（m） |
| --- | ---: |
| sci-radio-01 | -2.80 |
| sci-cray-01 | -2.20 |
| sci-astro-01 | -2.21 |
| com-l4-01 | -1.88 |
| sci-orbiter-01 | -5.00 |

另外有 2 个 DOM-dependent SKIP，以及 Node 的 MODULE_TYPELESS_PACKAGE_JSON 提示。原始输出已留存；本轮没有修改这些模块，也没有改验证阈值或包配置。不能报告“全库 validate 全绿”。

## 4. 浏览器烟测

在主城根目录启动（8123 保留，不占用）：

```powershell
Set-Location E:\Claude\mars
python -m http.server 8127 --bind 127.0.0.1
```

打开 `http://127.0.0.1:8127/dev/DELIVERY/_imperial/smoke-r7.html`。测试页真实加载 `viewer/index.html?imperial=1&debug=1`；用既有引擎场景、站位、地形与 `build(ctx)`，没有将独立预览的假地形结果冒充主城结果。

| 项目 | 实测 |
| --- | --- |
| 模块加载 | R7 成功；group 世界平移 `[1100,0,-800]` |
| 现有 GLB | 5 类全成功；10 个克隆 Mesh，额外 192,000 面；未改 GLB 文件 |
| 图层昼景稳态 | **113 draws / 120**；渲染提交 359,206 面（含 GLB 等，与程序化预算口径不同） |
| 图层夜景稳态 | **113 draws / 120**；相同相机、相同 GLB |
| 一次性初始化 | 25 draws = CubeCamera 6 + PMREM 19，独立列账，不隐藏在稳态中 |
| 水 | roughness=0.15，envMap 存在 |
| 图层日景 rAF | 约 92.2～131.5 fps，22 个预热后采样 |
| 图层夜景 rAF | 约 123.5～130.4 fps，20 个预热后采样 |
| 主城全场景 | 118／160 draws（轴视／斜视场景含其他主城内容）；约 77.8～125.0 fps |

fps 取各模式剔除前 3 个记录后的样本，视口 1265×675；完整原始样本含切换／编译低帧，未删去。该数字不是 4K、VR、任意设备或所有相机状态下的 60 fps 保证。隔离模式仅为核验图层预算，光照由测试页临时提供；不会写回主城文件。

主城 window error / unhandledrejection 监听为 0；浏览器日志另外记录到 **1 条无来源 URL 的 MutationObserver.observe TypeError**（06:23:09 UTC，重载附近）。在模块、main.js、烟测页内未找到该调用，但不足以确证来源，按未归因项原样保留，**不宣称浏览器控制台绝对零错误**。没有记录到 imperial load failed。

## 5. 总控待接入／未验收

1. **第一人称碰撞未通过。** 当前 `viewer/main.js` 的 `collectColliders()` 明确跳过 `isInstancedMesh`，而御道台阶／铺地使用实例化。通过主城 `__col.colFloorAt()` 对 157 个路径点检查：**157 个地面高度不匹配、0 个阻挡**。不能用“没有墙挡住”当作“能走上去”。需总控支持实例化行走面或专用路径碰撞，并复跑连续行走；本轮不改 main.js。未交付可宣称成功走完主城的 60 s 录屏。
2. **知识卡文件与锚点已交付，UI 未接线。** 主城 `loadImperial()` 目前不读取此卡；普通 `loadPois()` 又固定从 units 目录取卡。请总控针对图层读取 `group.userData.infoUrl`，使用 `poi_` 节点转世界坐标，并使显示随天宫层开关同步。本轮不往 manifest 塞入错误签名的普通资产。
3. **没有改地形。** 中轴连续性和中轴地形侵入已检查；整个 1181×953 m 周边与原始起伏地形的全部交界并未逐点验收，边缘融合仍应结合主城截图审看。
4. **反射是一次性近似探针。** 有金顶环境倒影，不是逐池精确镜像；昼夜只调整反射强度，不会实时重捕夜景。
5. **视觉证据为本次落地主城截图。** 不将本次交付标为逐条重新完成 ref/hero.jpg 全套美术终验。R7 的几何骨架沿用来源版本，当前轮重点是交付、坐标适配和可复现验收。

CHECKLIST 中本次未找到天宫城／imperial 的现成行，故未新增或改动其他团队的验收栏。主城当前包含并行工作流的无关改动；不做整库提交或清理。

## 6. 完整性

- 模块 SHA-256：`855d2e9bbbd76bce3f99671fee3ab350d5d4081de5ab17f6a99cd859af3cdc57`
- 知识卡 SHA-256：`200ea4205a1431e0f2aff56a8089555e785d4d68d1a3e5ba74019401ac2ca0ab`
- 未改动的 `viewer/main.js` SHA-256：`e5b3dc2b0199e2bbcefde6e41ccf7086f6d31cfebb3ce6bdd7503a7864094544`
- 地形高度场 SHA-256：`eb6608b770ac8b8c0b7f78c4c15bb4b002ebbdc14dbd203e741935f84e00c682`

其余证据哈希见同目录 `manifest-r7.json`。交付完成度：**模块／卡／专用 validate／Y 包络／三角形／渲染烟测已交；主城行走碰撞与卡片显示待总控。**
