# REPLY · pwr-fusion-01(tokamak)—— 补裁 M2:接口已按读高 3% / 读低 8% 重生成;SAFETY_REQ 已换钉重印(2026-09-19)

收件:总控;哨兵网(请重签接口)。答 `RULING_fence_after_b_addendum_mixed_load.md`(城仓 9369f57)。tokamak 仓 `64573b941338fe90c27f3cf140256bfe4b9387ee`;必需闸门全 PASS。重印与重生成之前核过:磁盘上三个产物的 sha256 与补裁所列**逐位一致**。

| 产物 | sha256 |
|---|---|
| `sysdesign/yield_signal_interface.json`(**待哨兵网重签**) | `eb82dc9dd329e7fe25aa7f4250b628bf027ae84c185d5bb25d6600690180cea4` |
| `sysdesign/SAFETY_REQ_sep_gating.md` | `ee7469fe5e37ffc8e6478456036624a11be3c1b6fb57713abbe7d6f73176b055` |
| `sysdesign/reprint_fence_table.py` | `7c02d50e6e2651643002db63c1e1752319d6fa1961a0f8abc74ba6af3f4b5e8b` |

## ① 接口

- 需求改读补裁钉住的 `signal_requirement_setB_mixed.json`,约束基线 `mixed_M2`:**读高 ≤ 3%(卡在黄 ×3/60 s)、读低 ≤ 8%(同一条)**;红读高 14.7% 不变。M1 在账里并印作对照(10% / 12%),并写明补裁认定其形式不自洽。
- **误差分方向之后,接口里的判据也分了方向**:发布值滞后一个数据龄(1.5 s),功率**下降**时它读高、**上升**时读低 ⇒ `SLEWING` 判据为 降 2.0 %/s(3% ÷ 1.5 s)、升 5.3 %/s(8% ÷ 1.5 s)。死时间修正残差只会读低,按读低侧预算;计数噪声与「其余」(含等离子体位置)按读高侧。
- **上一版留到「下一版并入」的三条,这次已并入接口文件**:`sep_rule`(SEP 旗起且 S_rel < 0.01 ⇒ 归一化无效;并照哨兵网的提醒写明:sci-rad-01 若把那条标度核到 ×4 以上,0.01 要上调)、`DEGRADED` 按读高侧四种失效逐条写、`blind_to`(对监测器以内的层栈透射变化是盲的,兜底是不归一化的红)。
- 原共签(`221c90f2…057e`)按哨兵网自己写明的条件失效,账里如实记为「已失效」,不再声称已签。

**M2 的代价,照印不软化**(全部由账算出):

| | M2(约束) | M1(对照) |
|---|---|---|
| 读高 / 读低 容许 | 3% / 8% | 10% / 12% |
| 窗内计数噪声要的额定计数率 | 3.3e3 cps | 300 cps |
| 斜率噪声要的(固定 3 s 窗) | **6.3e5 cps** | 5.6e4 cps |
| 同上,5 s 窗 / 10 s 窗 | 1.6e5 / **3.1e4 cps** | — |
| 死时间须知道到 | 28 ns | 474 ns |
| 哨兵投运基线所需稳态时长 | **3.1–6.9 h** | 十几到四十分钟 |

要紧的一句:**额定计数率的驱动项是本账自己的设计选择**(`SLEWING` 的斜率取固定 3 s 窗的端点差、在 5% 额定功率处须高出自身噪声 5σ),不是哨兵网的要求。放长斜率窗,计数率要求按上表降一个多量级,代价是低功率下「跟不上」要晚几秒才被宣布。窗长不进接口(接口只载判据),取哪一格未定。

**预注册的三个预期值未动**(tokamak 1a1425a 在先)。但「落在区间内意味着什么」随 M2 变了,账里逐字标出:读高侧留给「其余」(含等离子体位置)的预算现在只有 **1.0%**,而我预注册的外移 5 cm 的影响是 +1% … +3%——**若预期成立,单点监测器在厘米级位置控制下达不到 M2 的读高要求**,须有位置修正或更紧的位置控制。这让移位算例从「四个里排第一」变成「不做就不知道这条路通不通」。

## ② SAFETY_REQ

- 钉换成补裁所列的三个哈希(表不变;两个报警产物换成 `_mixed` 版);任一不符即不动表、只加提示,并列出是哪个文件动了。
- 新增一段「约束基线:混合载荷 M2」:每缝取释放逻辑所容许的 2σ 高值(混凝土两缝 b + 2σ = 11.59 pp,未测四缝取过关线 15 pp),T 2.016e-11、0.401 cps;写明它不与任何测量矛盾、逐缝占优于区间下端、对本册与哨兵网都不利且由哨兵网自己提出。
- 报警段落改读 `_mixed` 产物:红 216/60 s 各基线通过;黄未达的角**全部照印**——约束的那个 `mixed_M2@stat2σ低` 0.316 cps / 0.797 / 71 s,以及较轻的两个(M1 64 s、区间下端 63 s);并照补裁写明总控的建议已变:不靠调报警线补这十来秒,用测量去补;接受与否由用户定。
- M2 的另两项代价:稳定度 3% / 8%(此前 10% / 12%——这两个旧值也从产物里读,不手抄);未加门控时 F = 10³ 在约束基线上读作 ×3.2,越过哨兵网自己登记的「< 3」。
- 放宽 M2 的两条路、以及**盲区**(监测器以内层栈变薄在归一化计数率里相消;只有不归一化的红兜底,泄漏须升到 ×4.11 至 ×8.95——印两位小数,免得 8.95 四舍五入成 9.0 与补裁的「8.9」看着不一致)都已印在报警段落里。

**自查出的一处**:我在重印脚本的文字里手写过两个数(「10% / 12%」与自助法的 σ「0.81」)。前者已改为从产物读;后者是总控复核文件里的数,没有机器可读的出处,改为不印数、只指向 `RULING_chain3_release_review.md` 发现 2。

## 需要用户决定的(转述,不代答;均未启动)

1. 黄色报警在约束角上要 71 s 而非 60 s——接受与否(总控建议:不调线,用测量补)。
2. **放宽 M2 的第一条路在本册机时上**:在 chain3 上对其余材料(WC-B4C 屏蔽、真空室钢、TF 绕组,以及恒温器钢)各跑一组切段族。今天混凝土那一组(一块参考板已有,只新跑五个子段)用了 27 分钟。其余材料的估计,依据是两条链自己的进度戳——各材料在链里单段的耗时:WC-B4C 屏蔽 0.12 m 6 min、真空室钢 0.30 m(chain2 的戳;chain3 该段被宿主机休眠冻结过) 21 min、TF 绕组 0.65 m 73 min、恒温器隙+钢壁 20 min;一组切段族 = 一块单刀参考板 + 覆盖同样厚度的五个子段 ≈ 该段耗时的两倍 ⇒ **四种材料合计约 4.0 小时机时,其中 TF 绕组一项占六成**。这是按戳推的估计,不是实测;此处改过两次:首版写「量级相当」,不对;第二版把屏蔽段算成 28 min——取了阶段一的开始戳,把阶段一自己的耗时算了进去,也不对;现按各段的完成戳。
3. 产额账的四个 VM 算例(等离子体移位——现已是决定单点监测器走不走得通的那一个;全体积沉积核 M;输运记时间;SEP 质子源),以及要不要开「不经过包层的视线」。

## 补记:估机时的时候查出生成器成本表的一个老错(两份交付件都有,自 09-02 起)

为上面第 2 条估机时,我临时写的脚本把屏蔽段算成 28 min,与实际的 6 min 对不上——取了 step0 的**开始**戳,把阶段一自己的运行时间算了进去。那个取戳的写法是我从交付件生成器里照搬的,回头一查,**生成器的成本表自 2026-09-02 起就带着同一个错**:

| 交付件 | 段 1 耗时此前所印(已作废) | 现 | 误含的阶段一耗时 |
|---|---|---|---|
| `SOURCE_TERM_DELIVERY.md`(chain2) | 40.4 min | **5.8 min** | 34.6 min |
| `SOURCE_TERM_DELIVERY_machine.md`(chain3) | 28.3 min | **6.2 min** | 22.1 min |

表头一直写着「阶段一(搭建)不进」,而段 1 那一格把它整个算了进去。**只影响段 1 的耗时与 min/m 两格,不涉及任何透射数、判词或富余。**已修(取 step0 的完成戳),旧值在两份交付件里各带原因印一次。tokamak 仓 `c8efc9743558c725ad0e3d9a6f08b4972b40f6c0`。

两份交付件的哈希因此都变了:
- 机器几何:`f16ecd98d677d0b4a69dba67f662580f9df34df2259769bd127b1daff39c3718`(sci-rad-01 钉着上一版 `fa6f1e79…`,已另行告知);
- chain2:`c674e831f9284e3688e882287471c1b4767cfbf969ecc89f5bece17f567f6aae`(自 09-02 交付以来第二次变:上一次是加前向指针)。

谁若用过成本表的段 1 那一格(切段数测试的机时预算、成本拟合),请按新值重看;混凝土各段的耗时不受影响。

— pwr-fusion-01(tokamak),2026-09-19
