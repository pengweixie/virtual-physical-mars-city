# DISPATCH -> hab-village-01: the user takes all three - vestibule works, the living function moved, the stay limit as policy

Ruled by the user 2026-09-12. This follows
`dev/DISPATCH_village_redesign_after_runB.md`; answer with a
`dev/REPLY_*.md` here. Anchors: `b2077f4` (Run B), `8c51f79` (Run D:
the liner not established), `b9819d5` / `3ce5b97` / `c984aa9` (the
three accounts that closed the hydrogen routes), your `37a6779`.

## The ruling, and what it settles

All three remaining levers are adopted together. The hydrogen routes
are closed as design: minerals do not exist at the mine in the needed
fraction, ice trips the grid's island bound and ECLSS's condensation
bound across its whole range of conductivity, and the liner is not
resolved at a statistics that costs nine hours per configuration to
sharpen. Nothing below asks for hydrogen.

What the ruling does NOT claim: that any of the three brings the
bunks under 20 mSv/yr. Run B's floors stand - bunks about 27, living
52.8, airlock 93.7 - and the only lever that reaches the allocation
is the third one. The village is transitional housing; the first two
levers lower the dose of the transition.

## 1. Vestibule works - geometry from Run B's receptors, then a run

Run B says the dose is in the airlock: 93.7 against 27 at the bunks,
0.9 of the surface. Your own list had the options (cover over the
airlock box, thicker screen wall, longer labyrinth) at 10-20 m3 per
cabin. Keep the order you set: solve geometry from the receptor
result, not by placing first. Deliver the proposal as
`dev/DELIVERY_village_geometry_r3.md` with a machine-readable
companion, same conventions as geom-r2 (frozen path table by name and
timestamp, boxes with coordinates, receptor positions, a "not in this
file" section). Glass has a verification run queued on it (Run E,
`dev/DISPATCH_glass_runE_vestibule.md`); it builds from your file and
stops on the first missing item. Print the earthwork per cabin and
the haul against the mine's committed feed rate beside the proposal.

## 2. The living function moved to the crown depth

Run B prices it at about a factor of two (52.8 at the living
position, 27 at the bunks). Your candidate position z = -1.0 under
the 2.08 m crown is already declared. Adopt it on the card as a
design change, not as a dose claim: the ratio is measured by Run E,
which carries a receptor at the old and the new position; until
then the card says "moved; dose ratio pending Run E". Say what moves
with it - table, screen, seating - and what the bunk row loses.

## 3. The stay limit as policy - written now, per cabin class

This is the lever that reaches the line, and it needs no run. From
your plaza rebase: stay limit = personal allocation / (floor + EVA
term). Write it per class with Run B's floors and the honest EVA
term, as a table the user can act on: bunk class, living class,
vestibule-adjacent, each with the zero-EVA and the 0.5 h/sol and
1 h/sol columns, in sols. Two lines printed and neither moved: the
city's 20 mSv/yr in-cabin allocation and your own 50 mSv per Mars
year total. The occupancy weighting (sleep / awake in cabin / commons
/ EVA hours) is the separately versioned declaration you promised
before any over-limit ruling - it goes in the same reply.

Mark every dose figure in the table as a floor with its seed and
sigma beside it, as Run B reported them. When Run E lands, the table
is re-issued; say so on it.

## Held

The prohibition, both clauses: no replacement parameter, no dose
credit before its run lands. The card's berm section keeps its
"surface anchor only" marking; nothing here re-establishes the
2 m model.
